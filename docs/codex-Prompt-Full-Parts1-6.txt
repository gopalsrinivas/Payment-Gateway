Part-1 → Project Setup
Part-2 → Database Design
Part-3 → Backend APIs
Part-4 → Frontend
Part-5 → Razorpay Integration
Part-6 → DevOps

Part-1 → Project Setup and codex prompt and Part-2 database design and Codex prompt ni base ga teesukoni,
part-3-> Backend APIs base cheysukoni kuda And Part-4 → Frontend base cheysukoni and Part-5 → Razorpay Integration
Part-6 → DevOps specification mariyu separate Codex implementation prompt create cheyi.



# Payment Gateway Demo

## Software Requirement Specification (SRS)

Version: 1.0

---

# 1. Project Overview

## Project Name

Payment Gateway Demo using Razorpay Test Mode

## Purpose

The purpose of this project is to develop a small full-stack Payment Gateway Demo application that demonstrates Razorpay payment integration using Test Mode.

The application should allow users to register, log in, view products, add products to a cart, create orders, complete payments using Razorpay Checkout, verify payment signatures, and view order and payment history.

The application should be production-style but interview-friendly, focusing on clean architecture, modular code, authentication, validations, logging, security, REST APIs, payment verification, webhooks, Docker, and CI/CD practices.

Part 1 should create only the project foundation.

Part 1 should not implement complete product, cart, order, checkout, payment verification, payment history, refund, or webhook-processing functionality.

---

# 2. Objectives

The application should demonstrate:

* Clean full-stack project architecture
* REST API development
* JWT authentication
* Role-based authorization
* PostgreSQL database configuration
* Sequelize ORM configuration
* Razorpay SDK configuration
* Razorpay Test Mode setup
* Webhook raw-body foundation
* Centralized error handling
* Input validation
* Application logging
* Swagger documentation
* Docker deployment foundation
* Backend and frontend integration readiness
* Environment variable management
* Secure credential handling
* Interview-ready project structure

---

# 3. Technology Stack

## Backend

* Node.js
* Express.js
* Sequelize ORM
* PostgreSQL
* JavaScript

## Frontend

* Next.js
* React.js
* Tailwind CSS
* Axios

## Authentication

* JWT
* bcryptjs

## Validation

* express-validator
* React Hook Form

## Logging

* Winston Logger
* Morgan

## API Documentation

* Swagger
* OpenAPI

## Payment Gateway

* Razorpay Test Mode
* Razorpay Node SDK
* Razorpay Orders API
* Razorpay Checkout
* Razorpay Signature Verification
* Razorpay Webhooks

## Security

* Helmet
* CORS
* Password hashing
* JWT expiration
* Environment variables

## Containerization

* Docker
* Docker Compose

## Development Tools

* Git
* GitHub
* Postman
* ESLint
* Prettier
* Nodemon

---

# 4. Database

## Database Name

```text
payment_gateway_demo
```

## Database Engine

```text
PostgreSQL
```

Part 1 should create only the following tables:

```text
roles
users
```

Product, cart, order, payment, payment log, and webhook event tables should be created only in Part 2.

---

# 5. User Roles

## Admin

Admin can eventually:

* Manage products
* View all orders
* View all payments
* View payment logs
* View dashboard reports
* Manage application configuration

In Part 1, Admin role authentication and authorization foundation should be created.

## Customer

Customer can eventually:

* Register and log in
* View products
* Add products to cart
* Create orders
* Complete Razorpay payments
* View order history
* View payment history

In Part 1, Customer registration, login, profile, and logout functionality should be created.

---

# 6. Application Modules

The complete application should contain the following modules:

1. Authentication
2. User Profile
3. Role Authorization
4. Dashboard
5. Product Management
6. Cart Management
7. Order Management
8. Razorpay Order Creation
9. Razorpay Checkout
10. Payment Verification
11. Payment Failure Handling
12. Payment History
13. Webhook Processing
14. Payment Logs
15. API Logs
16. Swagger Documentation
17. Docker and CI/CD

Part 1 should implement only:

1. Project Setup
2. Backend Foundation
3. Frontend Foundation
4. PostgreSQL Configuration
5. Sequelize Configuration
6. Authentication Foundation
7. Role Authorization Foundation
8. Razorpay SDK Configuration Foundation
9. Webhook Raw-Body Foundation
10. Logging
11. Swagger
12. Docker Foundation
13. Health API
14. Basic Tests

---

# 7. Payment Flow

The complete payment flow should be:

```text
User Login
    ↓
View Products
    ↓
Add Products to Cart
    ↓
Create Application Order
    ↓
Backend Calculates Trusted Amount
    ↓
Backend Creates Razorpay Order
    ↓
Frontend Opens Razorpay Checkout
    ↓
Customer Completes Payment
    ↓
Razorpay Returns Payment Details
    ↓
Frontend Sends Payment Details to Backend
    ↓
Backend Verifies Payment Signature
    ↓
Backend Updates Payment Status
    ↓
Backend Updates Order Status
    ↓
Payment Success Page
```

Webhook flow:

```text
Razorpay
    ↓
Webhook Endpoint
    ↓
Read Exact Raw Request Body
    ↓
Verify Webhook Signature
    ↓
Store Webhook Event
    ↓
Prevent Duplicate Processing
    ↓
Update Payment Status
    ↓
Update Order Status
```

Part 1 should create only the Razorpay configuration and webhook route foundation.

Part 1 should not create actual Razorpay orders or process payments.

---

# 8. Authentication Requirements

Part 1 should implement the following APIs:

```text
POST /api/v1/auth/register
POST /api/v1/auth/login
POST /api/v1/auth/logout
GET  /api/v1/auth/profile
```

## Registration

Registration should:

* Validate name
* Validate email
* Validate password
* Require a minimum password length of 8 characters
* Prevent duplicate email registration
* Assign Customer role by default
* Hash the password
* Store the user in PostgreSQL
* Return safe user information
* Never return the password

## Login

Login should:

* Validate email and password
* Verify that the user exists
* Reject inactive users
* Compare the hashed password
* Generate a JWT token
* Return safe user information
* Return role information
* Never log or return the password

## Profile

Profile API should:

* Require a valid JWT
* Return the authenticated user
* Include role information
* Exclude the password

## Logout

JWT authentication is stateless.

Logout should:

* Return a success response from the backend
* Remove the token from the frontend
* Not implement JWT blacklisting in Part 1

---

# 9. Razorpay Configuration Foundation

Create the following backend configuration file:

```text
backend/src/config/razorpay.js
```

The Razorpay configuration should:

* Use Razorpay Test Mode only
* Load credentials from environment variables
* Initialize one reusable Razorpay client
* Validate missing configuration clearly
* Never expose the Razorpay Key Secret
* Never log the Razorpay Key Secret
* Never send the Razorpay Key Secret to the frontend
* Avoid calling live APIs during Part 1
* Keep the integration ready for Part 3 and Part 5

Required environment variables:

```text
RAZORPAY_KEY_ID
RAZORPAY_KEY_SECRET
RAZORPAY_WEBHOOK_SECRET
```

Only the Razorpay Key ID may be available to the frontend.

---

# 10. Webhook Foundation

Create the webhook route:

```text
POST /api/v1/webhooks/razorpay
```

The webhook foundation should:

* Support the exact raw request body
* Keep webhook parsing separate from normal JSON parsing
* Avoid breaking other JSON APIs
* Return a safe placeholder response
* Not mark any payment as successful
* Not create payment records
* Not process Razorpay events in Part 1
* Keep the route ready for later implementation

Webhook signature verification should use:

```text
RAZORPAY_WEBHOOK_SECRET
```

Complete webhook event processing should be implemented in Part 5.

---

# 11. Non-Functional Requirements

The application should:

* Use clean MVC-style architecture
* Use Route → Middleware → Controller → Service → Model flow
* Use reusable components
* Use REST APIs
* Use JWT authentication
* Use role-based authorization
* Use environment variables
* Use centralized error handling
* Use input validations
* Use structured logging
* Use PostgreSQL
* Use Sequelize ORM
* Use Docker
* Use Swagger
* Use async/await
* Use standard API responses
* Avoid exposing sensitive information
* Be responsive on desktop and mobile
* Remain simple and interview-friendly
* Avoid unnecessary enterprise complexity

---

# 12. Coding Standards

Every controller should contain only request and response handling.

Business logic should be written inside services.

Database operations should be written using Sequelize models.

Do not create a repository layer for this project.

Every API should have validation.

Every API should return a standard response format.

Controllers should pass errors to centralized error middleware using `next(error)`.

Do not write direct SQL queries inside controllers.

Do not write Sequelize queries directly inside controllers.

Use async/await everywhere.

Use environment variables for configuration.

Follow REST naming conventions.

Use transactions where multiple related database updates are required.

Never log:

* Passwords
* JWT tokens
* Razorpay Key Secret
* Razorpay Webhook Secret
* Complete sensitive request bodies
* Card information

Never trust payment amounts sent by the frontend.

The final payable amount should eventually be calculated using trusted product and order data from the backend.

---

# 13. Standard API Response Format

## Success Response

```json
{
  "success": true,
  "message": "Operation completed successfully",
  "data": {},
  "statusCode": 200
}
```

## Error Response

```json
{
  "success": false,
  "message": "Operation failed",
  "errors": [],
  "statusCode": 400,
  "requestId": "REQ-000001"
}
```

---

# 14. Backend Project Folder Structure

```text
backend/
│
├── src/
│   ├── config/
│   │   ├── database.js
│   │   ├── env.js
│   │   ├── logger.js
│   │   ├── razorpay.js
│   │   └── swagger.js
│   │
│   ├── controllers/
│   │   └── authController.js
│   │
│   ├── services/
│   │   └── authService.js
│   │
│   ├── models/
│   │   ├── index.js
│   │   ├── Role.js
│   │   └── User.js
│   │
│   ├── routes/
│   │   ├── index.js
│   │   ├── authRoutes.js
│   │   ├── healthRoutes.js
│   │   └── webhookRoutes.js
│   │
│   ├── middlewares/
│   │   ├── authMiddleware.js
│   │   ├── roleMiddleware.js
│   │   ├── validationMiddleware.js
│   │   ├── requestLoggerMiddleware.js
│   │   ├── rawBodyMiddleware.js
│   │   └── errorMiddleware.js
│   │
│   ├── validations/
│   │   └── authValidation.js
│   │
│   ├── utils/
│   │   ├── responseHandler.js
│   │   ├── jwt.js
│   │   ├── password.js
│   │   ├── requestId.js
│   │   └── constants.js
│   │
│   ├── migrations/
│   ├── seeders/
│   ├── logs/
│   ├── swagger/
│   ├── app.js
│   └── server.js
│
├── tests/
├── .env
├── .env.example
├── .sequelizerc
├── package.json
├── Dockerfile
└── README.md
```

Do not create the following backend modules in Part 1:

```text
Product
Cart
Order
Order Item
Payment
Payment Log
Webhook Event
Refund
```

---

# 15. Frontend Project Folder Structure

Use Next.js App Router.

```text
frontend/
│
├── public/
│
├── src/
│   ├── app/
│   │   ├── login/
│   │   │   └── page.js
│   │   ├── register/
│   │   │   └── page.js
│   │   ├── dashboard/
│   │   │   └── page.js
│   │   ├── layout.js
│   │   └── page.js
│   │
│   ├── components/
│   │   ├── layout/
│   │   └── ui/
│   │
│   ├── contexts/
│   │   └── AuthContext.js
│   │
│   ├── services/
│   │   ├── apiClient.js
│   │   └── authService.js
│   │
│   ├── hooks/
│   ├── utils/
│   ├── config/
│   └── styles/
│
├── .env.local
├── .env.example
├── package.json
├── next.config.mjs
├── Dockerfile
└── README.md
```

Part 1 frontend should create only:

* Registration page
* Login page
* Basic protected dashboard placeholder
* Authentication context
* Axios API client
* Logout functionality
* Basic responsive layout

Do not create these pages in Part 1:

```text
/products
/cart
/checkout
/orders
/payments
/payment-success
/payment-failed
```

---

# 16. Root Project Folder Structure

```text
payment_gateway_demo/
│
├── backend/
├── frontend/
├── docs/
├── postman/
├── scripts/
├── .github/
├── docker-compose.yml
├── .gitignore
├── .env.example
└── README.md
```

---

# 17. Environment Variables

## Backend Environment Variables

Create:

```text
backend/.env.example
```

Content:

```env
NODE_ENV=development
PORT=5000

DB_HOST=localhost
DB_PORT=5432
DB_NAME=payment_gateway_demo
DB_USER=postgres
DB_PASSWORD=replace_with_database_password

JWT_SECRET=replace_with_long_random_secret
JWT_EXPIRES_IN=1d

RAZORPAY_KEY_ID=replace_with_test_key_id
RAZORPAY_KEY_SECRET=replace_with_test_key_secret
RAZORPAY_WEBHOOK_SECRET=replace_with_webhook_secret

CORS_ORIGIN=http://localhost:3000
LOG_LEVEL=info
```

## Frontend Environment Variables

Create:

```text
frontend/.env.example
```

Content:

```env
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000/api/v1
NEXT_PUBLIC_RAZORPAY_KEY_ID=replace_with_test_key_id
```

Never add the Razorpay Key Secret or Webhook Secret to frontend environment variables.

## Root Environment Variables

Create:

```text
.env.example
```

Content:

```env
POSTGRES_DB=payment_gateway_demo
POSTGRES_USER=postgres
POSTGRES_PASSWORD=replace_with_database_password

BACKEND_PORT=5000
FRONTEND_PORT=3000
```

Do not commit real `.env` files.

---

# 18. PostgreSQL and Sequelize Setup

Configure Sequelize using environment variables.

Create migrations for:

```text
roles
users
```

Create seeders for:

```text
Admin
Customer
```

Create one local development Admin user using clearly documented demo credentials.

Do not use real production credentials.

Configure the following relationship:

```text
Role has many Users
User belongs to Role
```

Use soft delete where consistent with the project.

Use migrations and seeders instead of Sequelize automatic table synchronization.

Do not use:

```javascript
sequelize.sync({ alter: true });
```

for production-style schema management.

---

# 19. Role Table

Recommended fields:

```text
id
name
description
is_active
created_at
updated_at
deleted_at
```

Requirements:

* Role name must be unique
* Admin and Customer roles should be seeded
* Soft delete may be enabled
* Timestamps should be enabled

Complete audit columns will be standardized in Part 2.

---

# 20. User Table

Recommended fields:

```text
id
name
email
password
role_id
is_active
created_at
updated_at
deleted_at
```

Requirements:

* Email must be unique
* Password must be hashed
* Password should be excluded from normal responses
* User should belong to a role
* Only active users should be allowed to log in
* Soft delete may be enabled
* Customer role should be assigned by default during registration

Complete audit columns will be standardized in Part 2.

---

# 21. Logging Requirements

Use Winston Logger.

Log:

* Application startup
* Database connection success
* Database connection failure
* User registration
* User login
* Authentication failure
* Authorization failure
* Health checks where useful
* API errors
* Validation errors
* Razorpay configuration errors
* Webhook route access

Add a unique request ID to each request.

Do not log sensitive values.

---

# 22. Error Handling

Implement centralized error handling for:

* Validation errors
* Authentication errors
* Authorization errors
* Not-found errors
* Conflict errors
* Sequelize validation errors
* Sequelize unique constraint errors
* Database errors
* Razorpay configuration errors
* Webhook errors
* Internal server errors

Do not expose stack traces in production.

---

# 23. Swagger Requirements

Swagger should be available at:

```text
http://localhost:5000/api-docs
```

Use the OpenAPI server URL:

```text
http://localhost:5000/api/v1
```

Document the following endpoints:

```text
POST /auth/register
POST /auth/login
POST /auth/logout
GET  /auth/profile
GET  /health
POST /webhooks/razorpay
```

Swagger should include:

* Request body schemas
* Validation rules
* JWT bearer authentication
* Success response examples
* Error response examples
* Field-level validation errors
* Webhook foundation note

Clearly state that the Razorpay webhook endpoint is only a Part 1 foundation.

---

# 24. Health API

Implement:

```text
GET /api/v1/health
```

The response should include:

* Application status
* Current timestamp
* Environment
* Database connectivity status where practical

Do not expose secrets or database credentials.

---

# 25. Docker Foundation

Create:

```text
backend/Dockerfile
frontend/Dockerfile
docker-compose.yml
```

Docker Compose should contain:

```text
postgres
backend
frontend
```

Requirements:

* Use a named PostgreSQL volume
* Use a shared Docker network
* Configure health checks
* Use environment variables
* Use correct service hostnames
* Allow backend database retries
* Point frontend to the backend correctly
* Preserve local non-Docker development
* Do not include real secrets
* Use multi-stage builds where useful

Part 1 should create only a clean Docker foundation.

Advanced production deployment should be implemented in Part 6.

---

# 26. Testing Requirements

Add meaningful backend tests for:

* Successful user registration
* Duplicate registration rejection
* Successful login
* Invalid login rejection
* Inactive user login rejection
* Profile API without a token
* Profile API with a valid token
* Health endpoint
* Role middleware foundation
* Webhook placeholder endpoint

Use the existing Node.js test setup or Supertest.

Clean test-created database data safely.

Do not create payment tests in Part 1.

---

# 27. Security Requirements

The project should follow these rules:

* Use Razorpay Test Mode only
* Never expose the Razorpay Key Secret
* Never expose the Razorpay Webhook Secret
* Never hardcode credentials
* Never commit real `.env` files
* Hash passwords
* Use JWT expiration
* Enable Helmet
* Configure CORS
* Validate all inputs
* Do not log passwords or tokens
* Do not trust frontend payment amounts
* Keep payment verification on the backend
* Keep webhook verification on the backend
* Do not store card information
* Do not implement payment processing prematurely

---

# 28. Part-1 Deliverables

Part 1 should produce:

* Root project structure
* Backend application foundation
* Frontend application foundation
* PostgreSQL configuration
* Sequelize configuration
* Roles migration
* Users migration
* Admin and Customer role seeders
* Local development Admin user seeder
* Registration API
* Login API
* Logout API
* Protected profile API
* JWT utility
* Authentication middleware
* Role authorization middleware
* Razorpay SDK configuration foundation
* Webhook raw-body foundation
* Health API
* Centralized error handling
* Winston logging
* Request IDs
* Swagger setup
* Safe environment examples
* Backend Dockerfile
* Frontend Dockerfile
* Docker Compose foundation
* Backend tests
* Root README
* Backend README
* Frontend README

---

# 29. Part-1 Excluded Scope

Do not implement the following in Part 1:

* Product CRUD
* Product search
* Cart management
* Order creation
* Order items
* Razorpay Order API call
* Razorpay Checkout
* Payment signature verification
* Payment failure handling
* Payment history
* Webhook event processing
* Payment logs
* Refunds
* Dashboard reports
* Production cloud deployment
* Advanced CI/CD

These features belong to later parts.

---

# 30. Development Order

Follow this development order:

```text
1. Inspect the existing repository
2. Run git status
3. Preserve existing correct files
4. Create the root project structure
5. Initialize the backend
6. Initialize the frontend
7. Configure PostgreSQL
8. Configure Sequelize
9. Create roles migration
10. Create users migration
11. Create role seeders
12. Create development Admin user seeder
13. Configure model associations
14. Implement common utilities
15. Implement centralized error handling
16. Implement logging and request IDs
17. Implement registration
18. Implement login
19. Implement JWT middleware
20. Implement role middleware
21. Implement profile
22. Implement logout
23. Configure Razorpay SDK foundation
24. Configure webhook raw-body foundation
25. Implement health API
26. Configure Swagger
27. Create frontend authentication pages
28. Create authentication context
29. Create protected dashboard placeholder
30. Create Docker foundation
31. Add tests
32. Update documentation
33. Run complete verification
```

---

# 31. Part-1 Acceptance Criteria

Part 1 is complete only when:

* Backend starts successfully
* Frontend starts successfully
* PostgreSQL connection works
* Sequelize migrations run successfully
* Seeders run successfully
* Roles and Users tables are created
* Admin and Customer roles are seeded
* Local Admin account is created
* Registration API works
* Duplicate registration is rejected
* Login API works
* JWT token is returned
* Invalid login is rejected
* Protected profile API works
* Missing token is rejected
* Role middleware exists
* Swagger opens successfully
* Health API returns HTTP 200
* Razorpay SDK configuration exists
* Razorpay secrets remain backend-only
* Webhook raw-body foundation exists
* Normal JSON APIs continue to work
* Backend lint passes
* Backend tests pass
* Frontend lint passes
* Frontend production build passes
* Docker Compose configuration is valid
* No Product, Cart, Order, or Payment business modules are implemented

---

# 32. Codex Implementation Instructions

You are a Senior Full Stack Developer.

Build only Part 1 of the Payment Gateway Demo application.

Before making changes:

1. Inspect the complete repository.
2. Run `git status`.
3. Read the available project documentation.
4. Inspect all existing files.
5. Preserve correct existing implementation.
6. Do not overwrite correct files unnecessarily.
7. Identify conflicts before editing.
8. List the files expected to be created or modified.
9. Summarize the implementation plan.

During implementation:

* Follow the documented architecture.
* Keep controllers thin.
* Put business logic inside services.
* Use Sequelize models for database access.
* Use migrations and seeders.
* Use async/await.
* Use centralized error handling.
* Use standard API responses.
* Use validations for every API.
* Use safe logging.
* Keep the Razorpay Key Secret backend-only.
* Use Razorpay Test Mode only.
* Do not create payment business logic.
* Do not create incomplete Product, Cart, Order, or Payment modules.
* Avoid unnecessary enterprise complexity.
* Keep the project runnable locally and through Docker.

After implementation:

1. List all created files.
2. List all modified files.
3. Run backend lint.
4. Run backend tests.
5. Run frontend lint.
6. Run the frontend production build.
7. Validate Docker Compose.
8. Run Sequelize migrations.
9. Run Sequelize seeders.
10. Start the backend.
11. Verify the health API.
12. Verify Swagger.
13. Verify registration.
14. Verify duplicate registration rejection.
15. Verify login.
16. Verify invalid login rejection.
17. Verify the protected profile API.
18. Confirm the Razorpay configuration foundation exists.
19. Confirm the Razorpay Key Secret is not exposed.
20. Confirm the webhook raw-body foundation exists.
21. Confirm normal JSON APIs are not broken.
22. Confirm no payment business functionality was implemented.
23. Provide exact Windows CMD commands.
24. List all remaining manual steps.
25. Do not start Part 2 automatically.

Do not claim that a command passed unless it was actually executed successfully.

When a dependency, database, Docker service, Razorpay credential, or environment variable is unavailable, clearly report it as not configured or not verified.

---

# 33. Final Codex Output Format

Provide the final implementation report using the following headings:

```text
## Summary

## Repository Inspection

## Implementation Plan

## Files Created

## Files Modified

## Backend Setup

## Frontend Setup

## Database Setup

## Sequelize Migrations

## Seed Data

## Authentication

## Role Authorization

## Razorpay Configuration Foundation

## Webhook Foundation

## Logging

## Error Handling

## Swagger

## Docker

## Tests

## Verification Results

## Security Verification

## Windows CMD Commands

## Remaining Manual Steps

## Known Limitations

## Part 2 Readiness
```

---

# Next Part

Part 2 will contain:

* Complete Database Design
* Roles Table
* Users Table
* Products Table
* Cart Items Table
* Orders Table
* Order Items Table
* Payments Table
* Payment Logs Table
* Webhook Events Table
* Primary Keys
* Foreign Keys
* Relationships
* Constraints
* Unique Constraints
* Check Constraints
* Indexes
* Audit Fields
* `created_at`
* `created_by`
* `updated_at`
* `updated_by`
* Soft Delete
* `is_deleted`
* `deleted_at`
* `deleted_by`
* Seed Data
* Sequelize Models
* Sequelize Migrations
* Database Acceptance Criteria


Part-2:
--------

Read these files completely before making any changes:

1. `docs/Part-2-Database-Design-Payment-Gateway.md`
2. `docs/Codex-Prompt-Part-2-Database-Design-Payment-Gateway.md`

Continue from the existing Part 1 implementation.

Implement ONLY Part 2: database design, Sequelize models, migrations, associations, indexes, constraints, audit fields, soft delete support, and seed data.

Project root:

```text
D:\Workspace\professional\Interview_Tasks\Orfus\Payment-Gateway
```

Do not create another nested project folder.

Requirements:

* Do not recreate or overwrite correct existing Part 1 migrations, models, seeders, authentication files, database configuration, Docker files, Swagger configuration, or frontend files.
* Preserve the existing `roles` and `users` tables and their working authentication flow.
* Do not duplicate existing `roles` or `users` migrations.
* Extend the existing schema safely using new migrations where required.
* Reuse the existing Sequelize naming conventions and model structure.
* Do not generate backend business APIs yet.
* Do not generate frontend pages yet.
* Do not implement Razorpay API calls, Checkout, signature verification, webhook event processing, payment history APIs, or refunds.
* Do not use `sequelize.sync()` or `sequelize.sync({ alter: true })`.
* Use Sequelize migrations and seeders only.
* Do not modify the database schema outside the documented Part 2 scope.
* If a real implementation conflict requires a schema change, explain the issue before changing it.

Implement database support for:

* Roles
* Users
* Products
* Cart Items
* Orders
* Order Items
* Payments
* Payment Logs
* Webhook Events

Implement or safely extend the following Sequelize models:

* `Role`
* `User`
* `Product`
* `CartItem`
* `Order`
* `OrderItem`
* `Payment`
* `PaymentLog`
* `WebhookEvent`

Implement model associations:

* Role has many Users
* User belongs to Role
* User has many Cart Items
* Cart Item belongs to User
* Product has many Cart Items
* Cart Item belongs to Product
* User has many Orders
* Order belongs to User
* Order has many Order Items
* Order Item belongs to Order
* Product has many Order Items
* Order Item belongs to Product
* Order has many Payments
* Payment belongs to Order
* User has many Payments where applicable
* Payment belongs to User where applicable
* Payment has many Payment Logs
* Payment Log belongs to Payment
* Order may have many Payment Logs where useful
* Webhook Event may reference Payment and Order where applicable

Use this database flow:

```text
Sequelize Migration
→ PostgreSQL Table
→ Sequelize Model
→ Model Association
→ Seeder
→ Database Verification
```

## Common Audit and Soft Delete Fields

Add these fields to all applicable business tables:

```text
created_at
created_by
updated_at
updated_by
is_deleted
deleted_at
deleted_by
```

Rules:

* `created_at` should default to the current timestamp.
* `updated_at` should update when the record changes.
* `created_by`, `updated_by`, and `deleted_by` should reference `users.id` where appropriate.
* Audit user foreign keys must allow `NULL` for system-created data and seed data.
* `is_deleted` should default to `false`.
* `deleted_at` and `deleted_by` should remain `NULL` until soft deletion.
* Do not physically delete business records where soft delete is required.
* Existing Part 1 authentication must continue to work after adding audit columns.
* Do not introduce circular migration failures through audit foreign keys.
* If necessary, add audit foreign keys in separate migrations after core tables exist.

## Roles Table

Preserve the existing table.

Verify or extend safely with:

* `id`
* `name`
* `description`
* `is_active`
* audit fields
* soft delete fields

Constraints:

* Role name must be unique among active/non-deleted roles.
* Seeded roles must remain:

  * Admin
  * Customer

Do not create duplicate roles.

## Users Table

Preserve the existing table and authentication-compatible fields.

Verify or extend safely with:

* `id`
* `name`
* `email`
* `password`
* `role_id`
* `is_active`
* audit fields
* soft delete fields

Constraints:

* Email must be unique.
* Role foreign key must reference `roles.id`.
* Password must remain compatible with existing authentication.
* Existing local Admin login must continue to work.
* Do not return or log password values.
* Do not change existing password hashing behavior.

## Products Table

Create fields such as:

* `id`
* `name`
* `slug`
* `description`
* `sku`
* `price`
* `currency`
* `image_url`
* `is_active`
* audit fields
* soft delete fields

Constraints:

* Product name required.
* Slug unique.
* SKU unique where provided.
* Price must be greater than or equal to zero.
* Currency should default to `INR`.
* Use a precise decimal type for product price.
* Do not store floating-point currency values.
* Product soft delete should not remove historical order item data.

Indexes:

* Product slug
* Product SKU
* Product name
* `is_active`
* `is_deleted`
* Composite indexes useful for active product listing

## Cart Items Table

Create fields such as:

* `id`
* `user_id`
* `product_id`
* `quantity`
* audit fields
* soft delete fields

Constraints:

* User foreign key required.
* Product foreign key required.
* Quantity must be greater than zero.
* Prevent duplicate active cart rows for the same user and product.
* Use a unique composite constraint or partial unique index where supported.

Indexes:

* `user_id`
* `product_id`
* Composite index on `user_id`, `is_deleted`
* Composite uniqueness for active cart items

## Orders Table

Create fields such as:

* `id`
* `order_number`
* `user_id`
* `subtotal_amount`
* `total_amount`
* `currency`
* `status`
* `payment_status`
* `notes`
* audit fields
* soft delete fields

Recommended order statuses:

```text
PENDING
CONFIRMED
PROCESSING
COMPLETED
CANCELLED
FAILED
```

Recommended payment statuses:

```text
PENDING
AUTHORIZED
PAID
FAILED
REFUNDED
PARTIALLY_REFUNDED
```

Constraints:

* Order number must be unique.
* User foreign key required.
* Amounts must be greater than or equal to zero.
* Currency should default to `INR`.
* Status fields should use ENUMs or validated strings consistently.
* Orders should normally be preserved for audit and not physically deleted.

Indexes:

* `order_number`
* `user_id`
* `status`
* `payment_status`
* `created_at`
* Composite index for user order history
* Composite index for admin status filtering

## Order Items Table

Create fields such as:

* `id`
* `order_id`
* `product_id`
* `product_name`
* `product_sku`
* `unit_price`
* `quantity`
* `line_total`
* audit fields
* soft delete fields where applicable

Rules:

* Store product snapshot values such as name, SKU, and unit price.
* Historical order items must not depend only on current product values.
* Quantity must be greater than zero.
* Unit price and line total must be greater than or equal to zero.
* Order foreign key required.
* Product foreign key may use `RESTRICT` or `SET NULL` based on documented design.
* Do not cascade-delete historical order items when a product is soft-deleted.

Indexes:

* `order_id`
* `product_id`
* Composite index on `order_id`, `product_id`

## Payments Table

Create fields such as:

* `id`
* `order_id`
* `user_id`
* `provider`
* `razorpay_order_id`
* `razorpay_payment_id`
* `razorpay_signature`
* `amount`
* `currency`
* `status`
* `method`
* `failure_code`
* `failure_description`
* `paid_at`
* `verified_at`
* audit fields
* soft delete fields

Recommended statuses:

```text
CREATED
PENDING
AUTHORIZED
CAPTURED
FAILED
REFUNDED
PARTIALLY_REFUNDED
```

Requirements:

* Provider should default to `RAZORPAY`.
* `razorpay_order_id` should be indexed.
* `razorpay_payment_id` should be unique when present.
* Do not store card number, CVV, full card details, or banking credentials.
* Do not store secrets.
* Signature may be stored only if required for audit and must never be logged.
* Amount should use the documented currency representation consistently.
* Prevent duplicate payment processing using unique constraints and idempotency-friendly indexes.

Indexes:

* `order_id`
* `user_id`
* `razorpay_order_id`
* Unique `razorpay_payment_id`
* `status`
* `created_at`

## Payment Logs Table

Create fields such as:

* `id`
* `payment_id`
* `order_id`
* `event_type`
* `previous_status`
* `new_status`
* `message`
* `request_id`
* `metadata`
* audit fields

Requirements:

* Use PostgreSQL `JSONB` for metadata where appropriate.
* Do not store secrets, passwords, JWT tokens, card data, or webhook secrets.
* Logs should support payment troubleshooting and status transition history.
* Payment log records should generally not be soft-deleted.
* If the common audit pattern is required, keep deletion fields nullable and avoid deleting audit history.

Indexes:

* `payment_id`
* `order_id`
* `event_type`
* `request_id`
* `created_at`

## Webhook Events Table

Create fields such as:

* `id`
* `provider`
* `event_id`
* `event_type`
* `entity_type`
* `razorpay_order_id`
* `razorpay_payment_id`
* `payload`
* `signature`
* `signature_verified`
* `processing_status`
* `processed_at`
* `error_message`
* `retry_count`
* audit fields

Recommended processing statuses:

```text
RECEIVED
VERIFIED
PROCESSING
PROCESSED
FAILED
IGNORED
```

Requirements:

* Use `JSONB` for payload.
* `event_id` should be unique when Razorpay provides a reliable event ID.
* When no event ID is available, support an idempotency key or payload hash strategy.
* Prevent duplicate webhook processing.
* Signature verification will be implemented later, but schema must support it.
* Do not log or expose the webhook secret.
* Store only data necessary for audit and troubleshooting.

Indexes:

* `event_id`
* `event_type`
* `razorpay_order_id`
* `razorpay_payment_id`
* `processing_status`
* `created_at`

## Foreign Key Rules

Use intentional foreign key actions.

Examples:

* Role deletion should not orphan Users.
* User deletion should not physically delete Orders or Payments.
* Product deletion should not delete Order Items.
* Order deletion should normally be restricted or handled as soft delete.
* Order Items should remain available for historical records.
* Payment Logs and Webhook Events should remain available for audit.
* Use `CASCADE`, `RESTRICT`, `SET NULL`, or `NO ACTION` only after considering historical data preservation.

Document every non-default foreign key action.

## Constraints

Implement appropriate:

* Primary keys
* Foreign keys
* Unique constraints
* Not-null constraints
* Check constraints
* Default values
* ENUMs or validated status strings
* Composite unique constraints
* Currency amount checks
* Positive quantity checks
* Soft-delete-aware uniqueness where practical

Do not rely only on API validation.

Important business constraints should also exist at the database level.

## Indexes

Add indexes for:

* Foreign key columns
* Frequently searched fields
* Unique provider identifiers
* Order history
* Payment history
* Product listing
* Status filters
* Created date filters
* Soft-delete filters
* Webhook duplicate detection

Avoid unnecessary indexes.

Explain each composite or partial index.

## Seed Data

Preserve existing:

* Admin role
* Customer role
* Local Admin user

Add safe development seed data for:

* Sample products
* Optional sample customer user
* Optional cart items only if they do not interfere with tests
* Optional sample orders and payments only if explicitly required by the Part 2 document

Rules:

* Use clearly fake development data.
* Do not use real personal information.
* Do not use real Razorpay credentials or transaction IDs.
* Seeders should be safe to run in the documented environment.
* Avoid duplicate seed data.
* Do not break existing authentication tests.

## Model Requirements

Each Sequelize model should:

* Use the correct table name.
* Use snake_case database columns where consistent with the project.
* Use existing timestamp conventions.
* Configure soft delete consistently.
* Expose associations through `models/index.js`.
* Avoid duplicate association registration.
* Exclude sensitive fields where appropriate.
* Match the migrations exactly.
* Avoid hidden automatic schema changes.

## Migration Requirements

Migrations should:

* Run in dependency-safe order.
* Be reversible where practical.
* Preserve existing data.
* Avoid dropping working Part 1 tables.
* Avoid recreating `roles` and `users`.
* Add audit columns to existing tables through alter migrations.
* Create new tables in correct foreign key order.
* Create audit foreign keys only after required tables exist.
* Add indexes and constraints explicitly.
* Include safe `down` methods.
* Avoid destructive changes.

## Tests

Add meaningful database-focused tests for:

* Models load successfully.
* Associations are configured.
* Required fields reject invalid data.
* Unique constraints work.
* Check constraints work.
* Product price validation.
* Cart quantity validation.
* Duplicate cart item prevention.
* Order number uniqueness.
* Payment identifier uniqueness.
* Soft delete behavior.
* Audit field behavior where testable.
* Existing registration and login tests still pass.

Do not add Part 3 API tests yet.

## Documentation

Update or create documentation for:

* Complete table list
* ER relationships
* Audit fields
* Soft delete strategy
* Foreign key actions
* Constraints
* Indexes
* Seed data
* Migration order
* Database reset commands
* Local PostgreSQL commands
* Known limitations
* Part 3 readiness

Do not remove existing Part 1 documentation.

## Before Making Changes

1. Open the project root:

```cmd
cd /d D:\Workspace\professional\Interview_Tasks\Orfus\Payment-Gateway
```

2. Run `git status`.
3. Inspect the complete current repository.
4. Read both Part 2 documents completely.
5. Inspect existing:

   * migrations
   * models
   * model associations
   * seeders
   * database configuration
   * authentication service
   * authentication tests
   * Docker configuration
6. Verify the current Part 1 migrations and seed data.
7. Summarize the implementation plan.
8. List files expected to be created or modified.
9. Identify migration conflicts before editing.
10. Do not begin Part 3.

## After Implementation

1. List all created files.
2. List all modified files.
3. Run backend lint.
4. Run backend tests.
5. Run migrations.
6. Run seeders.
7. Check migration status.
8. Verify all tables exist.
9. Verify foreign keys.
10. Verify indexes.
11. Verify unique and check constraints.
12. Verify seeded roles and Admin user remain valid.
13. Verify sample products where seeded.
14. Verify existing register API.
15. Verify existing login API.
16. Verify existing profile API.
17. Confirm passwords and secrets are not exposed.
18. Confirm the frontend still builds.
19. Confirm Docker Compose remains valid.
20. Confirm no Part 3 backend business APIs were implemented.
21. Provide exact Windows CMD commands.
22. List remaining manual steps.
23. Do not start Part 3 automatically.

## Verification Commands

Run the applicable commands from the project root or backend folder:

```cmd
cd /d D:\Workspace\professional\Interview_Tasks\Orfus\Payment-Gateway\backend

npm run lint
npm test
npm run db:migrate
npm run db:migrate:status
npm run db:seed
```

Verify PostgreSQL schema using commands such as:

```cmd
psql -U gopal -h localhost -p 5432 -d payment_gateway_demo
```

Then verify:

```sql
\dt

SELECT * FROM "SequelizeMeta" ORDER BY name;

SELECT id, name FROM roles ORDER BY id;

SELECT id, name, email, role_id, is_active
FROM users
ORDER BY id;

SELECT id, name, sku, price, currency, is_active
FROM products
ORDER BY id;
```

Also inspect constraints and indexes using PostgreSQL metadata queries.

Do not expose passwords in the final report.

## Final Output Format

Provide the final implementation report using these headings:

```text
## Summary

## Repository Inspection

## Implementation Plan

## Files Created

## Files Modified

## Existing Part 1 Preservation

## Database Design

## Tables Created

## Tables Extended

## Sequelize Models

## Model Associations

## Migrations

## Audit Fields

## Soft Delete

## Constraints

## Indexes

## Foreign Key Actions

## Seed Data

## Tests

## Verification Results

## Existing Authentication Regression Check

## Security Verification

## Windows CMD Commands

## Remaining Manual Steps

## Known Limitations

## Part 3 Readiness
```

Do not claim that a command passed unless it was actually executed successfully.

When PostgreSQL, environment variables, dependencies, or services are unavailable, clearly report the affected check as not configured or not verified.

Do not make unrelated changes.

Do not start Part 3.
