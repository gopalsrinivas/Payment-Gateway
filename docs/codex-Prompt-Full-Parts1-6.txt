Part-1 → Project Setup
Part-2 → Database Design
Part-3 → Backend APIs
Part-4 → Frontend
Part-5 → Razorpay Integration
Part-6 → DevOps

Part-1 → Project Setup and codex prompt and Part-2 database design and Codex prompt ni base ga teesukoni,
part-3-> Backend APIs base cheysukoni kuda And Part-4 → Frontend base cheysukoni and Part-5 → Razorpay Integration
Part-6 → DevOps specification mariyu separate Codex implementation prompt create cheyi.



# Payment Gateway Demo

## Software Requirement Specification (SRS)

Version: 1.0

---

# 1. Project Overview

## Project Name

Payment Gateway Demo using Razorpay Test Mode

## Purpose

The purpose of this project is to develop a small full-stack Payment Gateway Demo application that demonstrates Razorpay payment integration using Test Mode.

The application should allow users to register, log in, view products, add products to a cart, create orders, complete payments using Razorpay Checkout, verify payment signatures, and view order and payment history.

The application should be production-style but interview-friendly, focusing on clean architecture, modular code, authentication, validations, logging, security, REST APIs, payment verification, webhooks, Docker, and CI/CD practices.

Part 1 should create only the project foundation.

Part 1 should not implement complete product, cart, order, checkout, payment verification, payment history, refund, or webhook-processing functionality.

---

# 2. Objectives

The application should demonstrate:

* Clean full-stack project architecture
* REST API development
* JWT authentication
* Role-based authorization
* PostgreSQL database configuration
* Sequelize ORM configuration
* Razorpay SDK configuration
* Razorpay Test Mode setup
* Webhook raw-body foundation
* Centralized error handling
* Input validation
* Application logging
* Swagger documentation
* Docker deployment foundation
* Backend and frontend integration readiness
* Environment variable management
* Secure credential handling
* Interview-ready project structure

---

# 3. Technology Stack

## Backend

* Node.js
* Express.js
* Sequelize ORM
* PostgreSQL
* JavaScript

## Frontend

* Next.js
* React.js
* Tailwind CSS
* Axios

## Authentication

* JWT
* bcryptjs

## Validation

* express-validator
* React Hook Form

## Logging

* Winston Logger
* Morgan

## API Documentation

* Swagger
* OpenAPI

## Payment Gateway

* Razorpay Test Mode
* Razorpay Node SDK
* Razorpay Orders API
* Razorpay Checkout
* Razorpay Signature Verification
* Razorpay Webhooks

## Security

* Helmet
* CORS
* Password hashing
* JWT expiration
* Environment variables

## Containerization

* Docker
* Docker Compose

## Development Tools

* Git
* GitHub
* Postman
* ESLint
* Prettier
* Nodemon

---

# 4. Database

## Database Name

```text
payment_gateway_demo
```

## Database Engine

```text
PostgreSQL
```

Part 1 should create only the following tables:

```text
roles
users
```

Product, cart, order, payment, payment log, and webhook event tables should be created only in Part 2.

---

# 5. User Roles

## Admin

Admin can eventually:

* Manage products
* View all orders
* View all payments
* View payment logs
* View dashboard reports
* Manage application configuration

In Part 1, Admin role authentication and authorization foundation should be created.

## Customer

Customer can eventually:

* Register and log in
* View products
* Add products to cart
* Create orders
* Complete Razorpay payments
* View order history
* View payment history

In Part 1, Customer registration, login, profile, and logout functionality should be created.

---

# 6. Application Modules

The complete application should contain the following modules:

1. Authentication
2. User Profile
3. Role Authorization
4. Dashboard
5. Product Management
6. Cart Management
7. Order Management
8. Razorpay Order Creation
9. Razorpay Checkout
10. Payment Verification
11. Payment Failure Handling
12. Payment History
13. Webhook Processing
14. Payment Logs
15. API Logs
16. Swagger Documentation
17. Docker and CI/CD

Part 1 should implement only:

1. Project Setup
2. Backend Foundation
3. Frontend Foundation
4. PostgreSQL Configuration
5. Sequelize Configuration
6. Authentication Foundation
7. Role Authorization Foundation
8. Razorpay SDK Configuration Foundation
9. Webhook Raw-Body Foundation
10. Logging
11. Swagger
12. Docker Foundation
13. Health API
14. Basic Tests

---

# 7. Payment Flow

The complete payment flow should be:

```text
User Login
    ↓
View Products
    ↓
Add Products to Cart
    ↓
Create Application Order
    ↓
Backend Calculates Trusted Amount
    ↓
Backend Creates Razorpay Order
    ↓
Frontend Opens Razorpay Checkout
    ↓
Customer Completes Payment
    ↓
Razorpay Returns Payment Details
    ↓
Frontend Sends Payment Details to Backend
    ↓
Backend Verifies Payment Signature
    ↓
Backend Updates Payment Status
    ↓
Backend Updates Order Status
    ↓
Payment Success Page
```

Webhook flow:

```text
Razorpay
    ↓
Webhook Endpoint
    ↓
Read Exact Raw Request Body
    ↓
Verify Webhook Signature
    ↓
Store Webhook Event
    ↓
Prevent Duplicate Processing
    ↓
Update Payment Status
    ↓
Update Order Status
```

Part 1 should create only the Razorpay configuration and webhook route foundation.

Part 1 should not create actual Razorpay orders or process payments.

---

# 8. Authentication Requirements

Part 1 should implement the following APIs:

```text
POST /api/v1/auth/register
POST /api/v1/auth/login
POST /api/v1/auth/logout
GET  /api/v1/auth/profile
```

## Registration

Registration should:

* Validate name
* Validate email
* Validate password
* Require a minimum password length of 8 characters
* Prevent duplicate email registration
* Assign Customer role by default
* Hash the password
* Store the user in PostgreSQL
* Return safe user information
* Never return the password

## Login

Login should:

* Validate email and password
* Verify that the user exists
* Reject inactive users
* Compare the hashed password
* Generate a JWT token
* Return safe user information
* Return role information
* Never log or return the password

## Profile

Profile API should:

* Require a valid JWT
* Return the authenticated user
* Include role information
* Exclude the password

## Logout

JWT authentication is stateless.

Logout should:

* Return a success response from the backend
* Remove the token from the frontend
* Not implement JWT blacklisting in Part 1

---

# 9. Razorpay Configuration Foundation

Create the following backend configuration file:

```text
backend/src/config/razorpay.js
```

The Razorpay configuration should:

* Use Razorpay Test Mode only
* Load credentials from environment variables
* Initialize one reusable Razorpay client
* Validate missing configuration clearly
* Never expose the Razorpay Key Secret
* Never log the Razorpay Key Secret
* Never send the Razorpay Key Secret to the frontend
* Avoid calling live APIs during Part 1
* Keep the integration ready for Part 3 and Part 5

Required environment variables:

```text
RAZORPAY_KEY_ID
RAZORPAY_KEY_SECRET
RAZORPAY_WEBHOOK_SECRET
```

Only the Razorpay Key ID may be available to the frontend.

---

# 10. Webhook Foundation

Create the webhook route:

```text
POST /api/v1/webhooks/razorpay
```

The webhook foundation should:

* Support the exact raw request body
* Keep webhook parsing separate from normal JSON parsing
* Avoid breaking other JSON APIs
* Return a safe placeholder response
* Not mark any payment as successful
* Not create payment records
* Not process Razorpay events in Part 1
* Keep the route ready for later implementation

Webhook signature verification should use:

```text
RAZORPAY_WEBHOOK_SECRET
```

Complete webhook event processing should be implemented in Part 5.

---

# 11. Non-Functional Requirements

The application should:

* Use clean MVC-style architecture
* Use Route → Middleware → Controller → Service → Model flow
* Use reusable components
* Use REST APIs
* Use JWT authentication
* Use role-based authorization
* Use environment variables
* Use centralized error handling
* Use input validations
* Use structured logging
* Use PostgreSQL
* Use Sequelize ORM
* Use Docker
* Use Swagger
* Use async/await
* Use standard API responses
* Avoid exposing sensitive information
* Be responsive on desktop and mobile
* Remain simple and interview-friendly
* Avoid unnecessary enterprise complexity

---

# 12. Coding Standards

Every controller should contain only request and response handling.

Business logic should be written inside services.

Database operations should be written using Sequelize models.

Do not create a repository layer for this project.

Every API should have validation.

Every API should return a standard response format.

Controllers should pass errors to centralized error middleware using `next(error)`.

Do not write direct SQL queries inside controllers.

Do not write Sequelize queries directly inside controllers.

Use async/await everywhere.

Use environment variables for configuration.

Follow REST naming conventions.

Use transactions where multiple related database updates are required.

Never log:

* Passwords
* JWT tokens
* Razorpay Key Secret
* Razorpay Webhook Secret
* Complete sensitive request bodies
* Card information

Never trust payment amounts sent by the frontend.

The final payable amount should eventually be calculated using trusted product and order data from the backend.

---

# 13. Standard API Response Format

## Success Response

```json
{
  "success": true,
  "message": "Operation completed successfully",
  "data": {},
  "statusCode": 200
}
```

## Error Response

```json
{
  "success": false,
  "message": "Operation failed",
  "errors": [],
  "statusCode": 400,
  "requestId": "REQ-000001"
}
```

---

# 14. Backend Project Folder Structure

```text
backend/
│
├── src/
│   ├── config/
│   │   ├── database.js
│   │   ├── env.js
│   │   ├── logger.js
│   │   ├── razorpay.js
│   │   └── swagger.js
│   │
│   ├── controllers/
│   │   └── authController.js
│   │
│   ├── services/
│   │   └── authService.js
│   │
│   ├── models/
│   │   ├── index.js
│   │   ├── Role.js
│   │   └── User.js
│   │
│   ├── routes/
│   │   ├── index.js
│   │   ├── authRoutes.js
│   │   ├── healthRoutes.js
│   │   └── webhookRoutes.js
│   │
│   ├── middlewares/
│   │   ├── authMiddleware.js
│   │   ├── roleMiddleware.js
│   │   ├── validationMiddleware.js
│   │   ├── requestLoggerMiddleware.js
│   │   ├── rawBodyMiddleware.js
│   │   └── errorMiddleware.js
│   │
│   ├── validations/
│   │   └── authValidation.js
│   │
│   ├── utils/
│   │   ├── responseHandler.js
│   │   ├── jwt.js
│   │   ├── password.js
│   │   ├── requestId.js
│   │   └── constants.js
│   │
│   ├── migrations/
│   ├── seeders/
│   ├── logs/
│   ├── swagger/
│   ├── app.js
│   └── server.js
│
├── tests/
├── .env
├── .env.example
├── .sequelizerc
├── package.json
├── Dockerfile
└── README.md
```

Do not create the following backend modules in Part 1:

```text
Product
Cart
Order
Order Item
Payment
Payment Log
Webhook Event
Refund
```

---

# 15. Frontend Project Folder Structure

Use Next.js App Router.

```text
frontend/
│
├── public/
│
├── src/
│   ├── app/
│   │   ├── login/
│   │   │   └── page.js
│   │   ├── register/
│   │   │   └── page.js
│   │   ├── dashboard/
│   │   │   └── page.js
│   │   ├── layout.js
│   │   └── page.js
│   │
│   ├── components/
│   │   ├── layout/
│   │   └── ui/
│   │
│   ├── contexts/
│   │   └── AuthContext.js
│   │
│   ├── services/
│   │   ├── apiClient.js
│   │   └── authService.js
│   │
│   ├── hooks/
│   ├── utils/
│   ├── config/
│   └── styles/
│
├── .env.local
├── .env.example
├── package.json
├── next.config.mjs
├── Dockerfile
└── README.md
```

Part 1 frontend should create only:

* Registration page
* Login page
* Basic protected dashboard placeholder
* Authentication context
* Axios API client
* Logout functionality
* Basic responsive layout

Do not create these pages in Part 1:

```text
/products
/cart
/checkout
/orders
/payments
/payment-success
/payment-failed
```

---

# 16. Root Project Folder Structure

```text
payment_gateway_demo/
│
├── backend/
├── frontend/
├── docs/
├── postman/
├── scripts/
├── .github/
├── docker-compose.yml
├── .gitignore
├── .env.example
└── README.md
```

---

# 17. Environment Variables

## Backend Environment Variables

Create:

```text
backend/.env.example
```

Content:

```env
NODE_ENV=development
PORT=5000

DB_HOST=localhost
DB_PORT=5432
DB_NAME=payment_gateway_demo
DB_USER=postgres
DB_PASSWORD=replace_with_database_password

JWT_SECRET=replace_with_long_random_secret
JWT_EXPIRES_IN=1d

RAZORPAY_KEY_ID=replace_with_test_key_id
RAZORPAY_KEY_SECRET=replace_with_test_key_secret
RAZORPAY_WEBHOOK_SECRET=replace_with_webhook_secret

CORS_ORIGIN=http://localhost:3000
LOG_LEVEL=info
```

## Frontend Environment Variables

Create:

```text
frontend/.env.example
```

Content:

```env
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000/api/v1
NEXT_PUBLIC_RAZORPAY_KEY_ID=replace_with_test_key_id
```

Never add the Razorpay Key Secret or Webhook Secret to frontend environment variables.

## Root Environment Variables

Create:

```text
.env.example
```

Content:

```env
POSTGRES_DB=payment_gateway_demo
POSTGRES_USER=postgres
POSTGRES_PASSWORD=replace_with_database_password

BACKEND_PORT=5000
FRONTEND_PORT=3000
```

Do not commit real `.env` files.

---

# 18. PostgreSQL and Sequelize Setup

Configure Sequelize using environment variables.

Create migrations for:

```text
roles
users
```

Create seeders for:

```text
Admin
Customer
```

Create one local development Admin user using clearly documented demo credentials.

Do not use real production credentials.

Configure the following relationship:

```text
Role has many Users
User belongs to Role
```

Use soft delete where consistent with the project.

Use migrations and seeders instead of Sequelize automatic table synchronization.

Do not use:

```javascript
sequelize.sync({ alter: true });
```

for production-style schema management.

---

# 19. Role Table

Recommended fields:

```text
id
name
description
is_active
created_at
updated_at
deleted_at
```

Requirements:

* Role name must be unique
* Admin and Customer roles should be seeded
* Soft delete may be enabled
* Timestamps should be enabled

Complete audit columns will be standardized in Part 2.

---

# 20. User Table

Recommended fields:

```text
id
name
email
password
role_id
is_active
created_at
updated_at
deleted_at
```

Requirements:

* Email must be unique
* Password must be hashed
* Password should be excluded from normal responses
* User should belong to a role
* Only active users should be allowed to log in
* Soft delete may be enabled
* Customer role should be assigned by default during registration

Complete audit columns will be standardized in Part 2.

---

# 21. Logging Requirements

Use Winston Logger.

Log:

* Application startup
* Database connection success
* Database connection failure
* User registration
* User login
* Authentication failure
* Authorization failure
* Health checks where useful
* API errors
* Validation errors
* Razorpay configuration errors
* Webhook route access

Add a unique request ID to each request.

Do not log sensitive values.

---

# 22. Error Handling

Implement centralized error handling for:

* Validation errors
* Authentication errors
* Authorization errors
* Not-found errors
* Conflict errors
* Sequelize validation errors
* Sequelize unique constraint errors
* Database errors
* Razorpay configuration errors
* Webhook errors
* Internal server errors

Do not expose stack traces in production.

---

# 23. Swagger Requirements

Swagger should be available at:

```text
http://localhost:5000/api-docs
```

Use the OpenAPI server URL:

```text
http://localhost:5000/api/v1
```

Document the following endpoints:

```text
POST /auth/register
POST /auth/login
POST /auth/logout
GET  /auth/profile
GET  /health
POST /webhooks/razorpay
```

Swagger should include:

* Request body schemas
* Validation rules
* JWT bearer authentication
* Success response examples
* Error response examples
* Field-level validation errors
* Webhook foundation note

Clearly state that the Razorpay webhook endpoint is only a Part 1 foundation.

---

# 24. Health API

Implement:

```text
GET /api/v1/health
```

The response should include:

* Application status
* Current timestamp
* Environment
* Database connectivity status where practical

Do not expose secrets or database credentials.

---

# 25. Docker Foundation

Create:

```text
backend/Dockerfile
frontend/Dockerfile
docker-compose.yml
```

Docker Compose should contain:

```text
postgres
backend
frontend
```

Requirements:

* Use a named PostgreSQL volume
* Use a shared Docker network
* Configure health checks
* Use environment variables
* Use correct service hostnames
* Allow backend database retries
* Point frontend to the backend correctly
* Preserve local non-Docker development
* Do not include real secrets
* Use multi-stage builds where useful

Part 1 should create only a clean Docker foundation.

Advanced production deployment should be implemented in Part 6.

---

# 26. Testing Requirements

Add meaningful backend tests for:

* Successful user registration
* Duplicate registration rejection
* Successful login
* Invalid login rejection
* Inactive user login rejection
* Profile API without a token
* Profile API with a valid token
* Health endpoint
* Role middleware foundation
* Webhook placeholder endpoint

Use the existing Node.js test setup or Supertest.

Clean test-created database data safely.

Do not create payment tests in Part 1.

---

# 27. Security Requirements

The project should follow these rules:

* Use Razorpay Test Mode only
* Never expose the Razorpay Key Secret
* Never expose the Razorpay Webhook Secret
* Never hardcode credentials
* Never commit real `.env` files
* Hash passwords
* Use JWT expiration
* Enable Helmet
* Configure CORS
* Validate all inputs
* Do not log passwords or tokens
* Do not trust frontend payment amounts
* Keep payment verification on the backend
* Keep webhook verification on the backend
* Do not store card information
* Do not implement payment processing prematurely

---

# 28. Part-1 Deliverables

Part 1 should produce:

* Root project structure
* Backend application foundation
* Frontend application foundation
* PostgreSQL configuration
* Sequelize configuration
* Roles migration
* Users migration
* Admin and Customer role seeders
* Local development Admin user seeder
* Registration API
* Login API
* Logout API
* Protected profile API
* JWT utility
* Authentication middleware
* Role authorization middleware
* Razorpay SDK configuration foundation
* Webhook raw-body foundation
* Health API
* Centralized error handling
* Winston logging
* Request IDs
* Swagger setup
* Safe environment examples
* Backend Dockerfile
* Frontend Dockerfile
* Docker Compose foundation
* Backend tests
* Root README
* Backend README
* Frontend README

---

# 29. Part-1 Excluded Scope

Do not implement the following in Part 1:

* Product CRUD
* Product search
* Cart management
* Order creation
* Order items
* Razorpay Order API call
* Razorpay Checkout
* Payment signature verification
* Payment failure handling
* Payment history
* Webhook event processing
* Payment logs
* Refunds
* Dashboard reports
* Production cloud deployment
* Advanced CI/CD

These features belong to later parts.

---

# 30. Development Order

Follow this development order:

```text
1. Inspect the existing repository
2. Run git status
3. Preserve existing correct files
4. Create the root project structure
5. Initialize the backend
6. Initialize the frontend
7. Configure PostgreSQL
8. Configure Sequelize
9. Create roles migration
10. Create users migration
11. Create role seeders
12. Create development Admin user seeder
13. Configure model associations
14. Implement common utilities
15. Implement centralized error handling
16. Implement logging and request IDs
17. Implement registration
18. Implement login
19. Implement JWT middleware
20. Implement role middleware
21. Implement profile
22. Implement logout
23. Configure Razorpay SDK foundation
24. Configure webhook raw-body foundation
25. Implement health API
26. Configure Swagger
27. Create frontend authentication pages
28. Create authentication context
29. Create protected dashboard placeholder
30. Create Docker foundation
31. Add tests
32. Update documentation
33. Run complete verification
```

---

# 31. Part-1 Acceptance Criteria

Part 1 is complete only when:

* Backend starts successfully
* Frontend starts successfully
* PostgreSQL connection works
* Sequelize migrations run successfully
* Seeders run successfully
* Roles and Users tables are created
* Admin and Customer roles are seeded
* Local Admin account is created
* Registration API works
* Duplicate registration is rejected
* Login API works
* JWT token is returned
* Invalid login is rejected
* Protected profile API works
* Missing token is rejected
* Role middleware exists
* Swagger opens successfully
* Health API returns HTTP 200
* Razorpay SDK configuration exists
* Razorpay secrets remain backend-only
* Webhook raw-body foundation exists
* Normal JSON APIs continue to work
* Backend lint passes
* Backend tests pass
* Frontend lint passes
* Frontend production build passes
* Docker Compose configuration is valid
* No Product, Cart, Order, or Payment business modules are implemented

---

# 32. Codex Implementation Instructions

You are a Senior Full Stack Developer.

Build only Part 1 of the Payment Gateway Demo application.

Before making changes:

1. Inspect the complete repository.
2. Run `git status`.
3. Read the available project documentation.
4. Inspect all existing files.
5. Preserve correct existing implementation.
6. Do not overwrite correct files unnecessarily.
7. Identify conflicts before editing.
8. List the files expected to be created or modified.
9. Summarize the implementation plan.

During implementation:

* Follow the documented architecture.
* Keep controllers thin.
* Put business logic inside services.
* Use Sequelize models for database access.
* Use migrations and seeders.
* Use async/await.
* Use centralized error handling.
* Use standard API responses.
* Use validations for every API.
* Use safe logging.
* Keep the Razorpay Key Secret backend-only.
* Use Razorpay Test Mode only.
* Do not create payment business logic.
* Do not create incomplete Product, Cart, Order, or Payment modules.
* Avoid unnecessary enterprise complexity.
* Keep the project runnable locally and through Docker.

After implementation:

1. List all created files.
2. List all modified files.
3. Run backend lint.
4. Run backend tests.
5. Run frontend lint.
6. Run the frontend production build.
7. Validate Docker Compose.
8. Run Sequelize migrations.
9. Run Sequelize seeders.
10. Start the backend.
11. Verify the health API.
12. Verify Swagger.
13. Verify registration.
14. Verify duplicate registration rejection.
15. Verify login.
16. Verify invalid login rejection.
17. Verify the protected profile API.
18. Confirm the Razorpay configuration foundation exists.
19. Confirm the Razorpay Key Secret is not exposed.
20. Confirm the webhook raw-body foundation exists.
21. Confirm normal JSON APIs are not broken.
22. Confirm no payment business functionality was implemented.
23. Provide exact Windows CMD commands.
24. List all remaining manual steps.
25. Do not start Part 2 automatically.

Do not claim that a command passed unless it was actually executed successfully.

When a dependency, database, Docker service, Razorpay credential, or environment variable is unavailable, clearly report it as not configured or not verified.

---

# 33. Final Codex Output Format

Provide the final implementation report using the following headings:

```text
## Summary

## Repository Inspection

## Implementation Plan

## Files Created

## Files Modified

## Backend Setup

## Frontend Setup

## Database Setup

## Sequelize Migrations

## Seed Data

## Authentication

## Role Authorization

## Razorpay Configuration Foundation

## Webhook Foundation

## Logging

## Error Handling

## Swagger

## Docker

## Tests

## Verification Results

## Security Verification

## Windows CMD Commands

## Remaining Manual Steps

## Known Limitations

## Part 2 Readiness
```

---

# Next Part

Part 2 will contain:

* Complete Database Design
* Roles Table
* Users Table
* Products Table
* Cart Items Table
* Orders Table
* Order Items Table
* Payments Table
* Payment Logs Table
* Webhook Events Table
* Primary Keys
* Foreign Keys
* Relationships
* Constraints
* Unique Constraints
* Check Constraints
* Indexes
* Audit Fields
* `created_at`
* `created_by`
* `updated_at`
* `updated_by`
* Soft Delete
* `is_deleted`
* `deleted_at`
* `deleted_by`
* Seed Data
* Sequelize Models
* Sequelize Migrations
* Database Acceptance Criteria


Part-2:
--------

Read these files completely before making any changes:

1. `docs/Part-2-Database-Design-Payment-Gateway.md`
2. `docs/Codex-Prompt-Part-2-Database-Design-Payment-Gateway.md`

Continue from the existing Part 1 implementation.

Implement ONLY Part 2: database design, Sequelize models, migrations, associations, indexes, constraints, audit fields, soft delete support, and seed data.

Project root:

```text
D:\Workspace\professional\Interview_Tasks\Orfus\Payment-Gateway
```

Do not create another nested project folder.

Requirements:

* Do not recreate or overwrite correct existing Part 1 migrations, models, seeders, authentication files, database configuration, Docker files, Swagger configuration, or frontend files.
* Preserve the existing `roles` and `users` tables and their working authentication flow.
* Do not duplicate existing `roles` or `users` migrations.
* Extend the existing schema safely using new migrations where required.
* Reuse the existing Sequelize naming conventions and model structure.
* Do not generate backend business APIs yet.
* Do not generate frontend pages yet.
* Do not implement Razorpay API calls, Checkout, signature verification, webhook event processing, payment history APIs, or refunds.
* Do not use `sequelize.sync()` or `sequelize.sync({ alter: true })`.
* Use Sequelize migrations and seeders only.
* Do not modify the database schema outside the documented Part 2 scope.
* If a real implementation conflict requires a schema change, explain the issue before changing it.

Implement database support for:

* Roles
* Users
* Products
* Cart Items
* Orders
* Order Items
* Payments
* Payment Logs
* Webhook Events

Implement or safely extend the following Sequelize models:

* `Role`
* `User`
* `Product`
* `CartItem`
* `Order`
* `OrderItem`
* `Payment`
* `PaymentLog`
* `WebhookEvent`

Implement model associations:

* Role has many Users
* User belongs to Role
* User has many Cart Items
* Cart Item belongs to User
* Product has many Cart Items
* Cart Item belongs to Product
* User has many Orders
* Order belongs to User
* Order has many Order Items
* Order Item belongs to Order
* Product has many Order Items
* Order Item belongs to Product
* Order has many Payments
* Payment belongs to Order
* User has many Payments where applicable
* Payment belongs to User where applicable
* Payment has many Payment Logs
* Payment Log belongs to Payment
* Order may have many Payment Logs where useful
* Webhook Event may reference Payment and Order where applicable

Use this database flow:

```text
Sequelize Migration
→ PostgreSQL Table
→ Sequelize Model
→ Model Association
→ Seeder
→ Database Verification
```

## Common Audit and Soft Delete Fields

Add these fields to all applicable business tables:

```text
created_at
created_by
updated_at
updated_by
is_deleted
deleted_at
deleted_by
```

Rules:

* `created_at` should default to the current timestamp.
* `updated_at` should update when the record changes.
* `created_by`, `updated_by`, and `deleted_by` should reference `users.id` where appropriate.
* Audit user foreign keys must allow `NULL` for system-created data and seed data.
* `is_deleted` should default to `false`.
* `deleted_at` and `deleted_by` should remain `NULL` until soft deletion.
* Do not physically delete business records where soft delete is required.
* Existing Part 1 authentication must continue to work after adding audit columns.
* Do not introduce circular migration failures through audit foreign keys.
* If necessary, add audit foreign keys in separate migrations after core tables exist.

## Roles Table

Preserve the existing table.

Verify or extend safely with:

* `id`
* `name`
* `description`
* `is_active`
* audit fields
* soft delete fields

Constraints:

* Role name must be unique among active/non-deleted roles.
* Seeded roles must remain:

  * Admin
  * Customer

Do not create duplicate roles.

## Users Table

Preserve the existing table and authentication-compatible fields.

Verify or extend safely with:

* `id`
* `name`
* `email`
* `password`
* `role_id`
* `is_active`
* audit fields
* soft delete fields

Constraints:

* Email must be unique.
* Role foreign key must reference `roles.id`.
* Password must remain compatible with existing authentication.
* Existing local Admin login must continue to work.
* Do not return or log password values.
* Do not change existing password hashing behavior.

## Products Table

Create fields such as:

* `id`
* `name`
* `slug`
* `description`
* `sku`
* `price`
* `currency`
* `image_url`
* `is_active`
* audit fields
* soft delete fields

Constraints:

* Product name required.
* Slug unique.
* SKU unique where provided.
* Price must be greater than or equal to zero.
* Currency should default to `INR`.
* Use a precise decimal type for product price.
* Do not store floating-point currency values.
* Product soft delete should not remove historical order item data.

Indexes:

* Product slug
* Product SKU
* Product name
* `is_active`
* `is_deleted`
* Composite indexes useful for active product listing

## Cart Items Table

Create fields such as:

* `id`
* `user_id`
* `product_id`
* `quantity`
* audit fields
* soft delete fields

Constraints:

* User foreign key required.
* Product foreign key required.
* Quantity must be greater than zero.
* Prevent duplicate active cart rows for the same user and product.
* Use a unique composite constraint or partial unique index where supported.

Indexes:

* `user_id`
* `product_id`
* Composite index on `user_id`, `is_deleted`
* Composite uniqueness for active cart items

## Orders Table

Create fields such as:

* `id`
* `order_number`
* `user_id`
* `subtotal_amount`
* `total_amount`
* `currency`
* `status`
* `payment_status`
* `notes`
* audit fields
* soft delete fields

Recommended order statuses:

```text
PENDING
CONFIRMED
PROCESSING
COMPLETED
CANCELLED
FAILED
```

Recommended payment statuses:

```text
PENDING
AUTHORIZED
PAID
FAILED
REFUNDED
PARTIALLY_REFUNDED
```

Constraints:

* Order number must be unique.
* User foreign key required.
* Amounts must be greater than or equal to zero.
* Currency should default to `INR`.
* Status fields should use ENUMs or validated strings consistently.
* Orders should normally be preserved for audit and not physically deleted.

Indexes:

* `order_number`
* `user_id`
* `status`
* `payment_status`
* `created_at`
* Composite index for user order history
* Composite index for admin status filtering

## Order Items Table

Create fields such as:

* `id`
* `order_id`
* `product_id`
* `product_name`
* `product_sku`
* `unit_price`
* `quantity`
* `line_total`
* audit fields
* soft delete fields where applicable

Rules:

* Store product snapshot values such as name, SKU, and unit price.
* Historical order items must not depend only on current product values.
* Quantity must be greater than zero.
* Unit price and line total must be greater than or equal to zero.
* Order foreign key required.
* Product foreign key may use `RESTRICT` or `SET NULL` based on documented design.
* Do not cascade-delete historical order items when a product is soft-deleted.

Indexes:

* `order_id`
* `product_id`
* Composite index on `order_id`, `product_id`

## Payments Table

Create fields such as:

* `id`
* `order_id`
* `user_id`
* `provider`
* `razorpay_order_id`
* `razorpay_payment_id`
* `razorpay_signature`
* `amount`
* `currency`
* `status`
* `method`
* `failure_code`
* `failure_description`
* `paid_at`
* `verified_at`
* audit fields
* soft delete fields

Recommended statuses:

```text
CREATED
PENDING
AUTHORIZED
CAPTURED
FAILED
REFUNDED
PARTIALLY_REFUNDED
```

Requirements:

* Provider should default to `RAZORPAY`.
* `razorpay_order_id` should be indexed.
* `razorpay_payment_id` should be unique when present.
* Do not store card number, CVV, full card details, or banking credentials.
* Do not store secrets.
* Signature may be stored only if required for audit and must never be logged.
* Amount should use the documented currency representation consistently.
* Prevent duplicate payment processing using unique constraints and idempotency-friendly indexes.

Indexes:

* `order_id`
* `user_id`
* `razorpay_order_id`
* Unique `razorpay_payment_id`
* `status`
* `created_at`

## Payment Logs Table

Create fields such as:

* `id`
* `payment_id`
* `order_id`
* `event_type`
* `previous_status`
* `new_status`
* `message`
* `request_id`
* `metadata`
* audit fields

Requirements:

* Use PostgreSQL `JSONB` for metadata where appropriate.
* Do not store secrets, passwords, JWT tokens, card data, or webhook secrets.
* Logs should support payment troubleshooting and status transition history.
* Payment log records should generally not be soft-deleted.
* If the common audit pattern is required, keep deletion fields nullable and avoid deleting audit history.

Indexes:

* `payment_id`
* `order_id`
* `event_type`
* `request_id`
* `created_at`

## Webhook Events Table

Create fields such as:

* `id`
* `provider`
* `event_id`
* `event_type`
* `entity_type`
* `razorpay_order_id`
* `razorpay_payment_id`
* `payload`
* `signature`
* `signature_verified`
* `processing_status`
* `processed_at`
* `error_message`
* `retry_count`
* audit fields

Recommended processing statuses:

```text
RECEIVED
VERIFIED
PROCESSING
PROCESSED
FAILED
IGNORED
```

Requirements:

* Use `JSONB` for payload.
* `event_id` should be unique when Razorpay provides a reliable event ID.
* When no event ID is available, support an idempotency key or payload hash strategy.
* Prevent duplicate webhook processing.
* Signature verification will be implemented later, but schema must support it.
* Do not log or expose the webhook secret.
* Store only data necessary for audit and troubleshooting.

Indexes:

* `event_id`
* `event_type`
* `razorpay_order_id`
* `razorpay_payment_id`
* `processing_status`
* `created_at`

## Foreign Key Rules

Use intentional foreign key actions.

Examples:

* Role deletion should not orphan Users.
* User deletion should not physically delete Orders or Payments.
* Product deletion should not delete Order Items.
* Order deletion should normally be restricted or handled as soft delete.
* Order Items should remain available for historical records.
* Payment Logs and Webhook Events should remain available for audit.
* Use `CASCADE`, `RESTRICT`, `SET NULL`, or `NO ACTION` only after considering historical data preservation.

Document every non-default foreign key action.

## Constraints

Implement appropriate:

* Primary keys
* Foreign keys
* Unique constraints
* Not-null constraints
* Check constraints
* Default values
* ENUMs or validated status strings
* Composite unique constraints
* Currency amount checks
* Positive quantity checks
* Soft-delete-aware uniqueness where practical

Do not rely only on API validation.

Important business constraints should also exist at the database level.

## Indexes

Add indexes for:

* Foreign key columns
* Frequently searched fields
* Unique provider identifiers
* Order history
* Payment history
* Product listing
* Status filters
* Created date filters
* Soft-delete filters
* Webhook duplicate detection

Avoid unnecessary indexes.

Explain each composite or partial index.

## Seed Data

Preserve existing:

* Admin role
* Customer role
* Local Admin user

Add safe development seed data for:

* Sample products
* Optional sample customer user
* Optional cart items only if they do not interfere with tests
* Optional sample orders and payments only if explicitly required by the Part 2 document

Rules:

* Use clearly fake development data.
* Do not use real personal information.
* Do not use real Razorpay credentials or transaction IDs.
* Seeders should be safe to run in the documented environment.
* Avoid duplicate seed data.
* Do not break existing authentication tests.

## Model Requirements

Each Sequelize model should:

* Use the correct table name.
* Use snake_case database columns where consistent with the project.
* Use existing timestamp conventions.
* Configure soft delete consistently.
* Expose associations through `models/index.js`.
* Avoid duplicate association registration.
* Exclude sensitive fields where appropriate.
* Match the migrations exactly.
* Avoid hidden automatic schema changes.

## Migration Requirements

Migrations should:

* Run in dependency-safe order.
* Be reversible where practical.
* Preserve existing data.
* Avoid dropping working Part 1 tables.
* Avoid recreating `roles` and `users`.
* Add audit columns to existing tables through alter migrations.
* Create new tables in correct foreign key order.
* Create audit foreign keys only after required tables exist.
* Add indexes and constraints explicitly.
* Include safe `down` methods.
* Avoid destructive changes.

## Tests

Add meaningful database-focused tests for:

* Models load successfully.
* Associations are configured.
* Required fields reject invalid data.
* Unique constraints work.
* Check constraints work.
* Product price validation.
* Cart quantity validation.
* Duplicate cart item prevention.
* Order number uniqueness.
* Payment identifier uniqueness.
* Soft delete behavior.
* Audit field behavior where testable.
* Existing registration and login tests still pass.

Do not add Part 3 API tests yet.

## Documentation

Update or create documentation for:

* Complete table list
* ER relationships
* Audit fields
* Soft delete strategy
* Foreign key actions
* Constraints
* Indexes
* Seed data
* Migration order
* Database reset commands
* Local PostgreSQL commands
* Known limitations
* Part 3 readiness

Do not remove existing Part 1 documentation.

## Before Making Changes

1. Open the project root:

```cmd
cd /d D:\Workspace\professional\Interview_Tasks\Orfus\Payment-Gateway
```

2. Run `git status`.
3. Inspect the complete current repository.
4. Read both Part 2 documents completely.
5. Inspect existing:

   * migrations
   * models
   * model associations
   * seeders
   * database configuration
   * authentication service
   * authentication tests
   * Docker configuration
6. Verify the current Part 1 migrations and seed data.
7. Summarize the implementation plan.
8. List files expected to be created or modified.
9. Identify migration conflicts before editing.
10. Do not begin Part 3.

## After Implementation

1. List all created files.
2. List all modified files.
3. Run backend lint.
4. Run backend tests.
5. Run migrations.
6. Run seeders.
7. Check migration status.
8. Verify all tables exist.
9. Verify foreign keys.
10. Verify indexes.
11. Verify unique and check constraints.
12. Verify seeded roles and Admin user remain valid.
13. Verify sample products where seeded.
14. Verify existing register API.
15. Verify existing login API.
16. Verify existing profile API.
17. Confirm passwords and secrets are not exposed.
18. Confirm the frontend still builds.
19. Confirm Docker Compose remains valid.
20. Confirm no Part 3 backend business APIs were implemented.
21. Provide exact Windows CMD commands.
22. List remaining manual steps.
23. Do not start Part 3 automatically.

## Verification Commands

Run the applicable commands from the project root or backend folder:

```cmd
cd /d D:\Workspace\professional\Interview_Tasks\Orfus\Payment-Gateway\backend

npm run lint
npm test
npm run db:migrate
npm run db:migrate:status
npm run db:seed
```

Verify PostgreSQL schema using commands such as:

```cmd
psql -U gopal -h localhost -p 5432 -d payment_gateway_demo
```

Then verify:

```sql
\dt

SELECT * FROM "SequelizeMeta" ORDER BY name;

SELECT id, name FROM roles ORDER BY id;

SELECT id, name, email, role_id, is_active
FROM users
ORDER BY id;

SELECT id, name, sku, price, currency, is_active
FROM products
ORDER BY id;
```

Also inspect constraints and indexes using PostgreSQL metadata queries.

Do not expose passwords in the final report.

## Final Output Format

Provide the final implementation report using these headings:

```text
## Summary

## Repository Inspection

## Implementation Plan

## Files Created

## Files Modified

## Existing Part 1 Preservation

## Database Design

## Tables Created

## Tables Extended

## Sequelize Models

## Model Associations

## Migrations

## Audit Fields

## Soft Delete

## Constraints

## Indexes

## Foreign Key Actions

## Seed Data

## Tests

## Verification Results

## Existing Authentication Regression Check

## Security Verification

## Windows CMD Commands

## Remaining Manual Steps

## Known Limitations

## Part 3 Readiness
```

Do not claim that a command passed unless it was actually executed successfully.

When PostgreSQL, environment variables, dependencies, or services are unavailable, clearly report the affected check as not configured or not verified.

Do not make unrelated changes.

Do not start Part 3.

Part-3:
--------
Read these files completely before making any changes:

1. `docs/Part-3-Backend-APIs-Payment-Gateway.md`
2. `docs/Codex-Prompt-Part-3-Backend-APIs-Payment-Gateway.md`

Continue from the existing Part 1 and Part 2 implementation.

Implement ONLY Part 3: backend APIs, backend architecture, validations, authorization, Swagger documentation, logging, and API tests.

Project root:

```text
D:\Workspace\professional\Interview_Tasks\Orfus\Payment-Gateway
```

Do not create another nested project folder.

## Existing Implementation

Part 1 is complete and includes:

* Backend and frontend project foundation
* PostgreSQL and Sequelize configuration
* Roles and Users tables
* Registration
* Login
* Logout
* Protected profile
* JWT authentication
* Role middleware
* Swagger
* Logging
* Centralized error handling
* Razorpay SDK configuration foundation
* Webhook raw-body route foundation
* Docker foundation
* Backend and frontend tests

Part 2 is complete and includes:

* Roles
* Users
* Products
* Cart Items
* Orders
* Order Items
* Payments
* Payment Logs
* Webhook Events
* Audit fields
* Soft delete
* Constraints
* Partial indexes
* Foreign keys
* Sequelize models
* Model associations
* Seed products
* Database tests

Preserve all working Part 1 and Part 2 functionality.

---

# Scope

Implement only backend business APIs for:

* Dashboard
* Products
* Cart
* Orders
* Payments
* Payment History
* Payment Logs
* Webhook administration or inspection where appropriate

Part 3 should prepare the backend for Part 4 frontend integration and Part 5 Razorpay integration.

Part 3 must not implement complete Razorpay payment processing.

---

# Important Restrictions

* Do not recreate or overwrite correct existing migrations.
* Do not recreate existing Sequelize models.
* Do not duplicate model associations.
* Do not remove or weaken database constraints.
* Do not modify the database schema unless required to fix a real implementation issue.
* Explain any required schema change before implementing it.
* Do not generate frontend business pages.
* Do not implement Razorpay Checkout.
* Do not implement real Razorpay Order API calls.
* Do not implement final payment signature verification.
* Do not implement full webhook event processing.
* Do not implement refunds.
* Do not implement production deployment.
* Do not use a repository layer.
* Do not use `sequelize.sync()`.
* Do not expose sensitive fields.
* Do not trust amounts sent by the frontend.

---

# Backend Architecture

Use this flow:

```text
Route
→ Validation Middleware
→ Authentication Middleware
→ Authorization Middleware
→ Controller
→ Service
→ Sequelize Model
→ PostgreSQL
```

Do not use a repository layer.

Controllers must:

* Accept `req`, `res`, and `next`.
* Use async functions.
* Handle only HTTP request and response concerns.
* Call service methods.
* Return standard response objects.
* Pass errors using `next(error)`.
* Not contain business logic.
* Not contain direct Sequelize queries.

Services must:

* Contain business logic.
* Use Sequelize models.
* Use transactions where required.
* Enforce ownership and role rules.
* Return plain data.
* Not access Express response objects.
* Not return sensitive fields.

Routes must:

* Apply validation.
* Apply authentication where required.
* Apply authorization where required.
* Use REST naming conventions.
* Be registered under `/api/v1`.

---

# Folder Structure

Preserve the existing backend structure.

Add or update files under:

```text
backend/src/
│
├── controllers/
├── services/
├── routes/
├── validations/
├── middlewares/
├── utils/
├── swagger/
└── app.js
```

Expected controller files may include:

```text
productController.js
cartController.js
orderController.js
paymentController.js
dashboardController.js
paymentLogController.js
```

Expected service files may include:

```text
productService.js
cartService.js
orderService.js
paymentService.js
dashboardService.js
paymentLogService.js
```

Expected route files may include:

```text
productRoutes.js
cartRoutes.js
orderRoutes.js
paymentRoutes.js
dashboardRoutes.js
paymentLogRoutes.js
```

Expected validation files may include:

```text
productValidation.js
cartValidation.js
orderValidation.js
paymentValidation.js
commonValidation.js
```

Reuse existing common utilities and middleware.

---

# Common API Prefix

Use:

```text
/api/v1
```

Register route groups:

```text
/api/v1/products
/api/v1/cart
/api/v1/orders
/api/v1/payments
/api/v1/payment-logs
/api/v1/dashboard
```

Preserve existing routes:

```text
/api/v1/auth
/api/v1/health
/api/v1/webhooks
```

---

# Standard API Response

Use the existing response helper.

Success response:

```json
{
  "success": true,
  "message": "Operation completed successfully",
  "data": {},
  "statusCode": 200
}
```

Error response:

```json
{
  "success": false,
  "message": "Operation failed",
  "errors": [],
  "statusCode": 400,
  "requestId": "REQ-000001"
}
```

List responses should include pagination metadata:

```json
{
  "success": true,
  "message": "Records retrieved successfully",
  "data": {
    "items": [],
    "pagination": {
      "page": 1,
      "limit": 10,
      "totalItems": 0,
      "totalPages": 0
    }
  },
  "statusCode": 200
}
```

---

# Authentication and Authorization

Reuse existing JWT authentication middleware.

Roles:

```text
Admin
Customer
```

Admin can:

* Create products
* Update products
* Soft-delete products
* View all products including inactive products when authorized
* View all orders
* View all payments
* View payment logs
* View dashboard totals

Customer can:

* View active products
* Manage only their own cart
* Create orders from their own cart
* View only their own orders
* View only their own payments
* View only their own payment history

Customers must not access:

* Other users’ cart records
* Other users’ orders
* Other users’ payment records
* Admin-only payment logs
* Admin dashboard data where restricted

Return HTTP 403 for unauthorized role access.

Return HTTP 404 instead of revealing another user’s protected record where appropriate.

---

# Audit Field Rules

Apply audit fields using the authenticated user.

For create operations:

```text
created_by = authenticated user ID
updated_by = authenticated user ID
```

For update operations:

```text
updated_by = authenticated user ID
```

For soft delete:

```text
is_deleted = true
deleted_at = current timestamp
deleted_by = authenticated user ID
updated_by = authenticated user ID
```

System-generated records may use nullable audit fields only where documented.

Do not trust audit user IDs from the frontend.

---

# Soft Delete Rules

Use the existing `is_deleted` strategy.

* Do not physically delete Products, Cart Items, Orders, Payments, or audit data unless explicitly documented.
* Default list APIs must exclude soft-deleted records.
* Admin-only APIs may optionally include deleted records using a validated filter.
* Historical Orders and Payments must remain available.
* Deleted Products must not disappear from existing Order Item snapshots.
* Cart item removal should use soft delete if consistent with the Part 2 model.
* Re-adding a soft-deleted cart product should restore or safely recreate the row according to existing database constraints.

---

# Search, Filtering, Sorting, and Pagination

Implement common query parameters:

```text
page
limit
search
sortBy
sortOrder
```

Rules:

* Default page: `1`
* Default limit: `10`
* Maximum limit: `100`
* Allowed sort order: `asc`, `desc`
* Validate allowed sort columns.
* Do not pass arbitrary frontend values directly into Sequelize order clauses.
* Use parameterized Sequelize operations.
* Return consistent pagination metadata.

Filtering should be implemented only for documented fields.

---

# Product APIs

Implement:

```text
GET    /api/v1/products
GET    /api/v1/products/:id
POST   /api/v1/products
PUT    /api/v1/products/:id
PATCH  /api/v1/products/:id/status
DELETE /api/v1/products/:id
```

## Product List

Access:

* Public or authenticated customer access may be allowed for active products.
* Admin can view all products based on validated filters.

Support:

* Pagination
* Search by name, SKU, or description
* Filter by active status
* Filter by currency where useful
* Sorting by name, price, created date, or updated date

Customer response should include only active, non-deleted products.

Admin may use:

```text
includeInactive=true
includeDeleted=true
```

only if explicitly authorized and validated.

## Product Details

* Return active product details to customers.
* Return full product details to Admin.
* Return 404 for deleted or unavailable products for customers.

## Create Product

Admin only.

Validate:

* Name required
* Name length
* Slug required or generate safely
* Slug uniqueness
* SKU uniqueness where provided
* Price required
* Price must be greater than or equal to zero
* Currency should default to INR
* Description length
* Image URL format where provided

Never accept `created_by`, `updated_by`, or delete fields from the request body.

## Update Product

Admin only.

* Support safe full or partial update according to the chosen route design.
* Do not update immutable historical Order Item values.
* Prevent duplicate slug or SKU.
* Apply `updated_by`.

## Product Status

Admin only.

Allow activation or deactivation.

Do not use status update as physical deletion.

## Delete Product

Admin only.

Use soft delete.

Do not delete existing order history.

Return conflict where the operation violates a documented business rule.

---

# Cart APIs

Implement:

```text
GET    /api/v1/cart
POST   /api/v1/cart/items
PATCH  /api/v1/cart/items/:id
DELETE /api/v1/cart/items/:id
DELETE /api/v1/cart
```

Customer authentication required.

## View Cart

Return only the authenticated customer’s active cart items.

Include:

* Product ID
* Product name
* Product SKU
* Unit price
* Quantity
* Line total
* Cart total
* Currency
* Cart item ID

Cart totals must be calculated from trusted current product data.

Do not trust price or amount from the frontend.

## Add Cart Item

Request body:

```json
{
  "productId": 1,
  "quantity": 2
}
```

Rules:

* Product must exist.
* Product must be active.
* Product must not be deleted.
* Quantity must be greater than zero.
* Enforce a reasonable maximum quantity.
* If the same active product already exists in the cart, update the quantity or return a clear conflict according to documented behavior.
* Handle previously soft-deleted cart rows safely.
* Apply audit fields.

## Update Cart Item

Request body:

```json
{
  "quantity": 3
}
```

Rules:

* Customer can update only their own cart item.
* Quantity must be positive.
* Product must remain active.
* Apply audit fields.

## Remove Cart Item

* Customer can remove only their own cart item.
* Use soft delete.
* Return 404 for inaccessible records.

## Clear Cart

* Soft-delete all active cart items for the authenticated customer.
* Use a transaction.
* Return the number of cleared items.

---

# Order APIs

Implement:

```text
POST /api/v1/orders
GET  /api/v1/orders
GET  /api/v1/orders/:id
GET  /api/v1/orders/:id/items
PATCH /api/v1/orders/:id/status
```

## Create Order

Customer authentication required.

Create an application order from the authenticated customer’s active cart.

Use a Sequelize transaction.

Required flow:

```text
Load active cart items
→ Lock or safely validate cart rows where practical
→ Load active product data
→ Validate products
→ Calculate trusted subtotal
→ Calculate trusted total
→ Generate unique application order number
→ Create Order
→ Create Order Item snapshots
→ Soft-delete or clear cart items
→ Commit transaction
```

Do not call Razorpay APIs in Part 3.

Order Item snapshots must include:

* Product name
* Product SKU
* Unit price
* Quantity
* Line total

Do not trust:

* Product price
* Subtotal
* Total amount
* Currency
* User ID
* Order number

from the frontend.

Initial order status:

```text
PENDING
```

Initial payment status:

```text
PENDING
```

Return the created application order.

## Order List

Customer:

* View only their own orders.

Admin:

* View all orders.

Support:

* Pagination
* Search by order number
* Filter by status
* Filter by payment status
* Filter by date range
* Sorting

## Order Details

Customer can view only their own order.

Admin can view any order.

Include:

* Order header
* Order Item snapshots
* Payment summary where available
* Audit-safe information

Do not expose sensitive payment fields.

## Order Status Update

Admin only.

Allowed transitions must be validated.

Example:

```text
PENDING → CONFIRMED
CONFIRMED → PROCESSING
PROCESSING → COMPLETED
PENDING → CANCELLED
CONFIRMED → CANCELLED
```

Do not allow arbitrary backward or invalid transitions.

Part 3 should not automatically mark an order paid.

Payment-related status changes belong to Part 5.

---

# Payment APIs

Implement database-backed APIs only.

Do not call Razorpay APIs in Part 3.

Implement:

```text
GET /api/v1/payments
GET /api/v1/payments/:id
GET /api/v1/orders/:orderId/payments
```

Optional internal preparation endpoint may be created only if documented and must not contact Razorpay.

Customer:

* View only their own payment records.
* View payments belonging to their own orders.

Admin:

* View all payment records.
* Filter and search payments.

Support filters:

* Payment status
* Provider
* Order ID
* Razorpay Order ID
* Razorpay Payment ID
* Date range

Never return:

* Full stored signature unless explicitly required
* Sensitive metadata
* Secrets
* Card information
* Banking credentials

Use safe response DTOs or serializers where practical.

---

# Payment History

Use the Payment model as the primary payment history source.

Implement:

```text
GET /api/v1/payments/history
```

Customer:

* Return only authenticated customer payment history.

Admin:

* May retrieve all payment history through the standard payment list endpoint.

Support:

* Pagination
* Status filter
* Date range
* Order number search
* Sorting

Return:

* Application order number
* Provider
* Amount
* Currency
* Status
* Method where available
* Created date
* Paid date where available

---

# Payment Log APIs

Implement Admin-only APIs:

```text
GET /api/v1/payment-logs
GET /api/v1/payment-logs/:id
GET /api/v1/payments/:paymentId/logs
```

Support:

* Pagination
* Payment ID
* Order ID
* Event type
* Request ID
* Date range
* Sorting

Do not expose:

* Secrets
* Passwords
* JWTs
* Raw card information
* Full sensitive request payloads

Payment logs should be read-only through public APIs.

Do not create manual log mutation APIs unless explicitly documented.

---

# Dashboard APIs

Implement:

```text
GET /api/v1/dashboard/customer
GET /api/v1/dashboard/admin
```

## Customer Dashboard

Customer authentication required.

Return only the authenticated customer’s summary:

* Active cart item count
* Total orders
* Pending orders
* Completed orders
* Successful payments
* Failed payments
* Pending payments
* Total paid amount

## Admin Dashboard

Admin only.

Return:

* Total active products
* Total customers
* Total orders
* Pending orders
* Completed orders
* Cancelled orders
* Successful payments
* Failed payments
* Pending payments
* Total collected amount
* Recent orders
* Recent payments

Use aggregate queries efficiently.

Exclude deleted records where appropriate.

Do not calculate revenue from failed or pending payments.

Define which payment statuses count as successful.

---

# Validation Requirements

Use `express-validator` or existing project validation patterns.

Validate:

* Path IDs as positive integers.
* Pagination parameters.
* Search lengths.
* Sort columns.
* Sort order.
* Date ranges.
* Product fields.
* Cart quantities.
* Order filters.
* Status values.
* Ownership-sensitive parameters.

Return field-level validation errors.

Do not allow unknown status values.

Do not accept audit fields from request bodies.

---

# Transactions and Concurrency

Use transactions for:

* Order creation
* Order Item creation
* Cart clearing after order creation
* Multi-record soft deletion
* Related status updates where multiple tables are changed

Use row locking or equivalent protection where useful for order creation.

Avoid duplicate order creation where the same request may be retried.

A simple idempotency strategy may be prepared only if documented.

Do not add unnecessary distributed-lock complexity.

---

# Error Handling

Reuse centralized error handling.

Handle:

* Validation errors
* Authentication errors
* Authorization errors
* Not-found errors
* Conflict errors
* Invalid status transitions
* Sequelize validation errors
* Sequelize unique constraint errors
* Foreign key errors
* Transaction failures
* Database errors
* Internal server errors

Do not expose stack traces in production.

---

# Logging

Reuse Winston and request ID middleware.

Log:

* Product create/update/delete
* Cart item add/update/remove
* Cart clear
* Order creation
* Order status update
* Payment history access where useful
* Admin payment log access
* Validation failures
* Authorization failures
* Transaction failures
* Database errors

Include:

* Request ID
* Authenticated user ID
* Action name
* Resource ID where available
* Safe status information

Do not log:

* Passwords
* JWT tokens
* Razorpay Key Secret
* Webhook Secret
* Full payment signatures
* Card information
* Sensitive request bodies

---

# Swagger Documentation

Update Swagger at:

```text
http://localhost:5000/api-docs/
```

Document all Part 3 APIs.

Include:

* Tags
* Route descriptions
* Authentication requirements
* Role requirements
* Query parameters
* Path parameters
* Request body schemas
* Response schemas
* Pagination schemas
* Validation errors
* Authorization errors
* Not-found responses
* Conflict responses
* Example payloads

Clearly mark that:

* Razorpay API integration is not implemented in Part 3.
* Payments are database-backed records only.
* Payment completion belongs to Part 5.

---

# API Tests

Add meaningful tests for:

## Products

* Customer can list active products.
* Admin can create product.
* Customer cannot create product.
* Duplicate SKU or slug rejected.
* Product validation works.
* Admin can update product.
* Admin can soft-delete product.
* Deleted product excluded from customer list.

## Cart

* Customer can add item.
* Duplicate item behavior works.
* Invalid quantity rejected.
* Inactive product rejected.
* Customer can view own cart.
* Customer cannot access another user’s item.
* Update quantity works.
* Remove item works.
* Clear cart works.
* Trusted totals are calculated from product data.

## Orders

* Empty cart order rejected.
* Order creation works.
* Order total calculated from product data.
* Order Items preserve product snapshots.
* Cart clears after order creation.
* Transaction rollback works on failure.
* Customer sees only own orders.
* Admin sees all orders.
* Unauthorized order access rejected.
* Invalid status transition rejected.
* Valid Admin status transition works.

## Payments

* Customer sees only own payments.
* Admin sees all payments.
* Payment details ownership is enforced.
* Sensitive fields excluded.
* Payment history pagination works.

## Dashboard

* Customer summary is scoped correctly.
* Admin summary requires Admin role.
* Aggregates exclude deleted records.
* Failed/pending payments are not counted as collected amount.

## Regression

Existing tests must continue to pass:

* Registration
* Login
* Profile
* Health
* Database model tests
* Soft-delete tests
* Constraint tests

Do not use real Razorpay API calls in tests.

---

# Documentation

Update or create documentation for:

* Backend API architecture
* Route list
* Role permissions
* Ownership rules
* Validation rules
* Product API examples
* Cart API examples
* Order creation flow
* Payment history behavior
* Dashboard response examples
* Error response examples
* Swagger URL
* Local testing commands
* Postman or curl examples
* Known limitations
* Part 4 readiness

Preserve existing Part 1 and Part 2 documentation.

---

# Before Making Changes

1. Open the project root:

```cmd
cd /d D:\Workspace\professional\Interview_Tasks\Orfus\Payment-Gateway
```

2. Run:

```cmd
git status
```

3. Inspect the complete repository.
4. Read both Part 3 files completely.
5. Inspect existing:

   * Routes
   * Controllers
   * Services
   * Middlewares
   * Validations
   * Models
   * Associations
   * Migrations
   * Seeders
   * Authentication flow
   * Response helpers
   * Error middleware
   * Request ID middleware
   * Winston logger
   * Swagger configuration
   * Existing tests
6. Confirm Part 1 and Part 2 are working.
7. Identify existing dirty files before making changes.
8. Summarize the implementation plan.
9. List files expected to be created or modified.
10. Identify any API or schema conflicts.
11. Do not start Part 4.

---

# After Implementation

1. List all created files.
2. List all modified files.
3. Run backend lint.
4. Run all backend tests.
5. Run migrations.
6. Check migration status.
7. Run seeders where needed.
8. Start the backend locally.
9. Verify health endpoint.
10. Verify Swagger.
11. Verify Admin login.
12. Verify Customer login.
13. Verify Product APIs.
14. Verify Cart APIs.
15. Verify Order APIs.
16. Verify Payment history APIs.
17. Verify Dashboard APIs.
18. Verify role restrictions.
19. Verify ownership restrictions.
20. Verify audit fields.
21. Verify soft delete.
22. Verify pagination, filtering, sorting, and search.
23. Verify frontend production build still passes.
24. Verify Docker Compose configuration remains valid.
25. Confirm no Razorpay payment processing was implemented.
26. Confirm no frontend business pages were implemented.
27. Provide exact Windows CMD commands.
28. List remaining manual steps.
29. Do not start Part 4 automatically.

---

# Suggested Verification Commands

From the backend folder:

```cmd
cd /d D:\Workspace\professional\Interview_Tasks\Orfus\Payment-Gateway\backend

npm install
npm run lint
npm run db:migrate
npm run db:migrate:status
npm run db:seed
npm test
npm run dev
```

Frontend regression build:

```cmd
cd /d D:\Workspace\professional\Interview_Tasks\Orfus\Payment-Gateway\frontend

npm run lint
npm run build
```

Docker configuration:

```cmd
cd /d D:\Workspace\professional\Interview_Tasks\Orfus\Payment-Gateway

docker compose config
```

Do not use Docker as the only verification path.

Local PostgreSQL, local backend, and frontend regression build should be verified first.

---

# Final Codex Output Format

Provide the final implementation report using these headings:

```text
## Summary

## Repository Inspection

## Implementation Plan

## Files Created

## Files Modified

## Existing Part 1 and Part 2 Preservation

## Backend Architecture

## Routes Registered

## Controllers

## Services

## Validations

## Authentication Integration

## Role Authorization

## Ownership Rules

## Product APIs

## Cart APIs

## Order APIs

## Payment APIs

## Payment History

## Payment Logs

## Dashboard APIs

## Audit Fields

## Soft Delete

## Transactions

## Search Filtering Sorting and Pagination

## Logging

## Error Handling

## Swagger

## Tests

## Verification Results

## Security Verification

## Frontend Regression Check

## Docker Configuration Check

## Windows CMD Commands

## Remaining Manual Steps

## Known Limitations

## Part 4 Readiness
```

Do not claim that a command, API, test, build, or verification passed unless it was actually executed successfully.

If PostgreSQL, credentials, services, dependencies, or environment variables are unavailable, report the affected verification as not configured or not verified.

Do not make unrelated changes.

Do not implement Part 4.

Do not implement full Razorpay integration.

Do not start Part 5.


Part-4 → Frontend
------------------
Read these files completely before making any changes:

1. `docs/Part-4-Frontend-Payment-Gateway.md`
2. `docs/Codex-Prompt-Part-4-Frontend-Payment-Gateway.md`

Continue from the existing Part 1, Part 2, and Part 3 implementation.

Implement ONLY Part 4: frontend application, frontend architecture, UI integration, authentication flow, role-based pages, API integration, Razorpay Checkout preparation, frontend validations, responsive design, and frontend tests.

Project root:

```text
D:\Workspace\professional\Interview_Tasks\Orfus\Payment-Gateway
```

Do not create another nested project folder.

# Existing Implementation

Part 1 is complete and includes:

* Root project setup
* Node.js and Express backend foundation
* Next.js frontend foundation
* PostgreSQL and Sequelize configuration
* Registration
* Login
* Logout
* Protected profile
* JWT authentication
* Role authorization foundation
* Swagger
* Logging
* Centralized error handling
* Razorpay SDK configuration foundation
* Webhook raw-body foundation
* Docker foundation
* Backend and frontend basic tests

Part 2 is complete and includes:

* Roles
* Users
* Products
* Cart Items
* Orders
* Order Items
* Payments
* Payment Logs
* Webhook Events
* Audit fields
* Soft delete
* Foreign keys
* Constraints
* Indexes
* Sequelize models
* Model associations
* Seed products
* Database tests

Part 3 is complete and includes backend APIs for:

* Products
* Cart
* Orders
* Payments
* Payment History
* Payment Logs
* Customer Dashboard
* Admin Dashboard
* Search
* Filtering
* Sorting
* Pagination
* Role authorization
* Ownership validation
* Swagger
* Postman collection
* Backend API tests

Preserve all working Part 1, Part 2, and Part 3 functionality.

---

# Scope

Implement only the frontend for:

* Authentication
* Customer Dashboard
* Admin Dashboard
* Product Listing
* Product Details
* Admin Product Management
* Cart
* Checkout preparation
* Order History
* Order Details
* Payment History
* Payment Details where supported
* Payment Success page foundation
* Payment Failure page foundation
* Admin Payment Logs
* Profile
* Navigation
* Loading states
* Empty states
* Error states
* Responsive UI

Part 4 should integrate only with the existing Part 3 backend APIs.

Part 4 must not implement full Razorpay payment processing.

---

# Important Restrictions

* Do not recreate or overwrite correct backend files.
* Do not modify existing database migrations.
* Do not modify Sequelize models unless required to fix a real frontend-blocking API issue.
* Do not change backend API contracts without first identifying and explaining the mismatch.
* Do not implement Razorpay Order API calls.
* Do not implement final Razorpay Checkout payment processing.
* Do not implement payment signature verification.
* Do not implement full webhook processing.
* Do not implement refunds.
* Do not implement production DevOps.
* Do not expose Razorpay Key Secret.
* Do not expose Razorpay Webhook Secret.
* Do not trust frontend totals.
* Do not duplicate existing API logic in the frontend.
* Do not use mock data when a working backend API exists.
* Do not use the Pages Router.
* Use Next.js App Router only.
* Do not introduce unnecessary state-management libraries.
* Reuse the existing Axios client, Auth Context, utilities, and UI patterns.

---

# Frontend Architecture

Use this flow:

```text
Page
→ Reusable Component
→ Context or Custom Hook
→ Frontend Service
→ Axios API Client
→ Backend REST API
```

Frontend pages must not call Axios directly where a reusable service is appropriate.

Use:

* Next.js App Router
* React
* JavaScript
* Tailwind CSS
* Axios
* React Hook Form
* React Hot Toast
* React Icons
* Existing project dependencies

Do not add Redux unless the existing architecture clearly requires it.

Use React Context for:

* Authentication
* Cart state where useful

Use local component state for page-specific state.

---

# Frontend Folder Structure

Preserve and extend the existing frontend structure.

Expected structure:

```text
frontend/
│
├── public/
│
├── src/
│   ├── app/
│   │   ├── login/
│   │   ├── register/
│   │   ├── dashboard/
│   │   ├── products/
│   │   │   ├── page.js
│   │   │   └── [id]/
│   │   │       └── page.js
│   │   ├── cart/
│   │   │   └── page.js
│   │   ├── checkout/
│   │   │   └── page.js
│   │   ├── orders/
│   │   │   ├── page.js
│   │   │   └── [id]/
│   │   │       └── page.js
│   │   ├── payments/
│   │   │   ├── page.js
│   │   │   └── [id]/
│   │   │       └── page.js
│   │   ├── payment-success/
│   │   │   └── page.js
│   │   ├── payment-failed/
│   │   │   └── page.js
│   │   ├── profile/
│   │   │   └── page.js
│   │   ├── admin/
│   │   │   ├── dashboard/
│   │   │   ├── products/
│   │   │   │   ├── page.js
│   │   │   │   ├── create/
│   │   │   │   │   └── page.js
│   │   │   │   └── [id]/
│   │   │   │       └── edit/
│   │   │   │           └── page.js
│   │   │   ├── orders/
│   │   │   ├── payments/
│   │   │   └── payment-logs/
│   │   ├── layout.js
│   │   └── page.js
│   │
│   ├── components/
│   │   ├── layout/
│   │   │   ├── Navbar.js
│   │   │   ├── Sidebar.js
│   │   │   ├── Footer.js
│   │   │   ├── CustomerLayout.js
│   │   │   └── AdminLayout.js
│   │   ├── auth/
│   │   │   ├── ProtectedRoute.js
│   │   │   └── RoleRoute.js
│   │   ├── products/
│   │   │   ├── ProductCard.js
│   │   │   ├── ProductList.js
│   │   │   ├── ProductForm.js
│   │   │   └── ProductFilters.js
│   │   ├── cart/
│   │   │   ├── CartItem.js
│   │   │   └── CartSummary.js
│   │   ├── orders/
│   │   │   ├── OrderCard.js
│   │   │   ├── OrderTable.js
│   │   │   └── OrderStatusBadge.js
│   │   ├── payments/
│   │   │   ├── PaymentTable.js
│   │   │   ├── PaymentStatusBadge.js
│   │   │   └── PaymentSummary.js
│   │   ├── dashboard/
│   │   │   ├── StatCard.js
│   │   │   ├── RecentOrders.js
│   │   │   └── RecentPayments.js
│   │   └── ui/
│   │       ├── Button.js
│   │       ├── Input.js
│   │       ├── Select.js
│   │       ├── Modal.js
│   │       ├── Table.js
│   │       ├── Pagination.js
│   │       ├── Loader.js
│   │       ├── EmptyState.js
│   │       ├── ErrorState.js
│   │       └── ConfirmDialog.js
│   │
│   ├── contexts/
│   │   ├── AuthContext.js
│   │   └── CartContext.js
│   │
│   ├── services/
│   │   ├── apiClient.js
│   │   ├── authService.js
│   │   ├── productService.js
│   │   ├── cartService.js
│   │   ├── orderService.js
│   │   ├── paymentService.js
│   │   ├── dashboardService.js
│   │   └── paymentLogService.js
│   │
│   ├── hooks/
│   │   ├── useAuth.js
│   │   ├── useCart.js
│   │   ├── usePagination.js
│   │   └── useDebounce.js
│   │
│   ├── utils/
│   │   ├── formatCurrency.js
│   │   ├── formatDate.js
│   │   ├── authStorage.js
│   │   ├── constants.js
│   │   └── errorMessage.js
│   │
│   ├── config/
│   │   └── appConfig.js
│   │
│   └── styles/
│
├── .env.local
├── .env.example
├── package.json
├── next.config.mjs
├── Dockerfile
└── README.md
```

Adapt names to the existing project conventions where required.

Do not duplicate correct existing files.

---

# Environment Variables

Use:

```env
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000/api/v1
NEXT_PUBLIC_RAZORPAY_KEY_ID=replace_with_test_key_id
```

Rules:

* Only `NEXT_PUBLIC_RAZORPAY_KEY_ID` may be available to the browser.
* Never add Razorpay Key Secret.
* Never add Razorpay Webhook Secret.
* Never add database credentials.
* Never hardcode production URLs.
* Preserve `.env.example`.
* Do not commit `.env.local`.

---

# API Client

Reuse or improve the existing Axios client.

The Axios client should:

* Use `NEXT_PUBLIC_API_BASE_URL`.
* Add the JWT token to authenticated requests.
* Handle JSON responses.
* Handle standard API error responses.
* Handle HTTP 401 consistently.
* Clear invalid authentication where appropriate.
* Redirect to login where appropriate.
* Preserve request ID from error responses.
* Avoid logging tokens.
* Avoid infinite interceptor loops.
* Not assume every error has the same response shape.

Use a reusable error-message helper.

---

# Authentication

Preserve existing:

* Registration
* Login
* Logout
* Profile
* Auth Context
* Token persistence
* Protected dashboard

Verify and improve:

* Loading state while authentication initializes.
* Safe token storage using the existing project approach.
* Automatic profile fetch.
* Logout clears authentication state.
* Unauthorized API responses clear invalid session.
* Protected routes redirect unauthenticated users.
* Role-protected routes restrict Customer and Admin pages.
* Refreshing a protected page should preserve authentication where possible.

Do not store passwords.

Do not display JWT tokens.

---

# Role-Based Navigation

Roles:

```text
Admin
Customer
```

Customer navigation:

* Dashboard
* Products
* Cart
* Orders
* Payments
* Profile
* Logout

Admin navigation:

* Admin Dashboard
* Products
* Orders
* Payments
* Payment Logs
* Profile
* Logout

Admin should not automatically use customer pages where a separate Admin view exists.

Customer must not see Admin links.

Customer entering an Admin URL manually should receive a safe forbidden state or redirect.

---

# Home Page

Create or improve the home page.

It should include:

* Project name
* Short description
* Razorpay Test Mode notice
* Login and Register actions for unauthenticated users
* Dashboard action for authenticated users
* Product browsing action
* Interview-demo styling
* Responsive layout

Do not claim real payment processing is active in Part 4.

---

# Product Listing

Page:

```text
/products
```

Use:

```text
GET /api/v1/products
```

Implement:

* Product cards or table
* Pagination
* Search
* Sort
* Loading state
* Empty state
* Error state
* Product image fallback
* Price formatting
* Active product filtering
* Product details navigation
* Add-to-cart action for authenticated Customers

Search should use debouncing where useful.

Do not load all records if pagination exists.

Do not trust frontend price as a final checkout amount.

---

# Product Details

Page:

```text
/products/[id]
```

Use:

```text
GET /api/v1/products/:id
```

Display:

* Product name
* SKU
* Description
* Price
* Currency
* Image
* Availability
* Quantity selector
* Add-to-cart action

Rules:

* Quantity must be positive.
* Product must be active.
* Handle product not found.
* Customer must log in before adding to cart.
* Admin may view details without Customer cart actions.

---

# Admin Product Management

Pages:

```text
/admin/products
/admin/products/create
/admin/products/[id]/edit
```

Use:

```text
GET    /api/v1/products
POST   /api/v1/products
PUT    /api/v1/products/:id
PATCH  /api/v1/products/:id/status
DELETE /api/v1/products/:id
```

Implement:

* Admin-only route protection
* Product table
* Search
* Pagination
* Status filter
* Create product form
* Edit product form
* Activate/deactivate action
* Soft-delete confirmation
* Validation errors
* Success/error toasts
* Loading states
* Empty states

Product form fields:

* Name
* Slug
* SKU
* Description
* Price
* Currency
* Image URL
* Active status

Never send audit fields.

Never send delete fields.

Do not expose internal database fields unnecessarily.

---

# Cart

Page:

```text
/cart
```

Use:

```text
GET    /api/v1/cart
POST   /api/v1/cart/items
PATCH  /api/v1/cart/items/:id
DELETE /api/v1/cart/items/:id
DELETE /api/v1/cart
```

Implement:

* Customer-only route
* Cart item list
* Product image
* Product name
* Unit price
* Quantity control
* Line total
* Cart total
* Remove action
* Clear cart action
* Checkout button
* Loading state
* Empty cart state
* Error handling

Rules:

* Use backend-returned totals.
* Do not calculate a trusted final amount only in the browser.
* Prevent quantity below 1.
* Prevent rapid duplicate submissions.
* Disable controls while requests are running.
* Refresh Cart Context after mutations.
* Display backend validation errors.

---

# Cart Context

Create or extend `CartContext`.

It should manage:

* Cart data
* Cart item count
* Cart total
* Cart loading state
* Add item
* Update item
* Remove item
* Clear cart
* Refresh cart

Rules:

* Cart should load only for authenticated Customers.
* Clear cart state on logout.
* Avoid duplicate API requests.
* Avoid stale cart count after mutations.
* Handle 401 responses safely.

---

# Checkout Preparation

Page:

```text
/checkout
```

Part 4 checkout is only frontend preparation.

Implement:

* Customer-only route
* Current cart summary
* Backend-returned item totals
* Backend-returned cart total
* Currency display
* Order creation action
* Test Mode notice
* Disabled or clearly labeled payment action until Part 5
* Order creation confirmation
* Redirect to created order details where appropriate

Use:

```text
POST /api/v1/orders
```

Required flow:

```text
Load Current Cart
→ Show Trusted Backend Cart Summary
→ Customer Confirms
→ Create Application Order
→ Receive Order Response
→ Clear Frontend Cart State
→ Redirect to Order Details
```

Do not open Razorpay Checkout in Part 4 unless the documentation explicitly requires only a safe placeholder integration.

Do not mark an order paid.

Do not send price, total, user ID, or payment status from the frontend.

---

# Orders

Pages:

```text
/orders
/orders/[id]
```

Use:

```text
GET /api/v1/orders
GET /api/v1/orders/:id
GET /api/v1/orders/:id/items
```

Implement order list:

* Customer sees own orders
* Admin may use separate Admin order page
* Pagination
* Search by order number
* Filter by status
* Filter by payment status
* Date filters where supported
* Status badges
* Payment status badges
* Empty state
* Loading state
* Error state

Order details should display:

* Order number
* Created date
* Status
* Payment status
* Currency
* Subtotal
* Total
* Order Item snapshots
* Product name
* Product SKU
* Unit price
* Quantity
* Line total
* Available payment summary

Do not expose sensitive payment fields.

---

# Admin Orders

Page:

```text
/admin/orders
```

Admin only.

Use:

```text
GET /api/v1/orders
PATCH /api/v1/orders/:id/status
```

Implement:

* All-order table
* Search
* Pagination
* Status filter
* Payment status filter
* Date filter
* Customer information where returned safely
* Order details action
* Valid status transition action
* Confirmation modal
* Success/error toast

Do not offer invalid status transitions in the UI.

Backend remains the final authority for transitions.

---

# Payment History

Page:

```text
/payments
```

Use:

```text
GET /api/v1/payments/history
GET /api/v1/payments
GET /api/v1/payments/:id
GET /api/v1/orders/:orderId/payments
```

Customer should see only their own payment history.

Display:

* Order number
* Provider
* Amount
* Currency
* Status
* Method
* Created date
* Paid date
* Details action where supported

Do not display:

* Full Razorpay signature
* Secrets
* Card numbers
* CVV
* Internal metadata
* Sensitive raw payloads

Empty payment history is valid in Part 4.

---

# Admin Payments

Page:

```text
/admin/payments
```

Admin only.

Implement:

* Payment table
* Pagination
* Status filter
* Provider filter
* Order search
* Razorpay identifier search where backend supports it
* Date range
* Payment details view
* Safe fields only

Part 4 must not add payment mutation actions.

---

# Payment Logs

Page:

```text
/admin/payment-logs
```

Admin only.

Use:

```text
GET /api/v1/payment-logs
GET /api/v1/payment-logs/:id
GET /api/v1/payments/:paymentId/logs
```

Implement:

* Read-only table
* Pagination
* Event type filter
* Payment filter
* Order filter
* Request ID filter
* Date filter
* Safe metadata display
* Details modal where useful

Never display secrets or card data.

---

# Customer Dashboard

Page:

```text
/dashboard
```

Use:

```text
GET /api/v1/dashboard/customer
```

Display:

* Active cart item count
* Total orders
* Pending orders
* Completed orders
* Successful payments
* Failed payments
* Pending payments
* Total paid amount
* Quick links
* Recent activity where returned

Customer dashboard must use only the authenticated Customer’s data.

---

# Admin Dashboard

Page:

```text
/admin/dashboard
```

Use:

```text
GET /api/v1/dashboard/admin
```

Display:

* Total active products
* Total customers
* Total orders
* Pending orders
* Completed orders
* Cancelled orders
* Successful payments
* Failed payments
* Pending payments
* Total collected amount
* Recent orders
* Recent payments

Use reusable stat cards and tables.

Do not count failed or pending payments as collected revenue.

---

# Profile

Page:

```text
/profile
```

Use the existing authenticated profile API.

Display:

* Name
* Email
* Role
* Active status
* Created date where returned

Do not show password or token.

Do not implement profile update unless the backend supports it.

---

# Payment Success Page Foundation

Page:

```text
/payment-success
```

Part 4 should include a safe page foundation only.

Display:

* Success-page layout
* Order reference if safely provided
* Button to Orders
* Button to Payment History
* Test Mode notice

Do not mark a payment successful based only on URL query parameters.

Do not trust frontend state as payment verification.

Final success routing belongs to Part 5 after backend verification.

---

# Payment Failure Page Foundation

Page:

```text
/payment-failed
```

Display:

* Failure message
* Safe failure reason if returned
* Order reference where available
* Retry guidance
* Button to Orders
* Button to Cart or Products

Do not expose raw gateway errors.

Do not create new payment records from this page.

---

# Loading, Empty, and Error States

Every data page should include:

* Initial loading state
* Mutation loading state
* Empty state
* API error state
* Retry action where useful
* Toast feedback
* Disabled submit buttons
* Duplicate-submission prevention

Do not leave blank pages while loading.

Do not show raw stack traces.

---

# Form Validation

Use React Hook Form.

Implement client-side validation for usability, but backend validation remains authoritative.

Validate:

* Required fields
* Email
* Password requirements
* Product name
* Product slug
* SKU
* Price
* Currency
* Quantity
* URL fields
* Search lengths
* Pagination values where manually entered

Display field-level backend validation errors where available.

Do not silently ignore API errors.

---

# Pagination

Create a reusable Pagination component.

Support:

* Current page
* Total pages
* Previous
* Next
* Page numbers where practical
* Disabled states
* Query parameter synchronization where useful

Preserve filters and search during page changes.

Do not request invalid page numbers.

---

# Search and Filters

Create reusable search/filter patterns.

Use:

* Debounced search
* Validated sort fields
* Validated sort order
* Status filters
* Date filters where supported
* Reset filters action

Avoid sending empty or invalid query values.

---

# Responsive Design

The frontend should work on:

* Desktop
* Tablet
* Mobile

Requirements:

* Responsive navigation
* Mobile menu
* Responsive cards
* Horizontally scrollable tables where necessary
* Accessible form labels
* Clear buttons
* Visible focus states
* Proper spacing
* Readable font sizes
* Consistent page width

Do not create a desktop-only layout.

---

# Accessibility

Implement:

* Semantic HTML
* Form labels
* Button text
* Keyboard navigation
* Focus states
* Accessible modal behavior where practical
* `aria-label` where needed
* Meaningful image alt text
* Status text in addition to colors
* Error messages associated with fields

---

# Security Requirements

* Never store or display Razorpay Key Secret.
* Never store or display Razorpay Webhook Secret.
* Never store passwords.
* Never log JWT tokens.
* Never expose full payment signatures.
* Never trust frontend totals.
* Never mark payment success from frontend state.
* Never allow UI role checks to replace backend authorization.
* Do not use `dangerouslySetInnerHTML` for API content.
* Do not render untrusted HTML.
* Sanitize or safely render user-controlled strings.
* Use backend responses as the source of truth.
* Handle 401 and 403 separately.

---

# Frontend Tests

Add meaningful tests using the existing frontend test setup.

If no frontend test framework exists, add a lightweight compatible setup only if appropriate.

Test:

## Authentication

* Login form validation
* Registration validation
* Protected route redirect
* Admin route restriction
* Logout clears session

## Products

* Product list loading
* Product list success
* Product empty state
* Product API error
* Admin product form validation
* Customer cannot access Admin product page

## Cart

* Add item action
* Quantity update
* Remove item
* Clear cart
* Empty cart
* Backend total display
* Duplicate-submit prevention

## Orders

* Order list rendering
* Order details rendering
* Empty order state
* Order API error
* Admin status action role protection

## Payments

* Empty payment history
* Payment list rendering
* Sensitive fields not rendered

## Dashboards

* Customer dashboard data
* Admin dashboard route protection
* Loading and error states

Mock backend requests.

Do not call real Razorpay APIs.

Do not require live PostgreSQL for frontend unit tests.

---

# Swagger and Backend Contract Review

Before implementing UI:

1. Inspect Swagger.
2. Inspect backend route files.
3. Inspect request and response shapes.
4. Inspect pagination structure.
5. Inspect error structure.
6. Inspect role restrictions.
7. Inspect ownership rules.
8. Inspect field names.
9. Inspect status values.
10. Inspect whether IDs are numbers or strings.

Do not guess API field names.

If frontend documentation and backend implementation differ, treat the working backend and Swagger as the source of truth and document the mismatch.

Do not change the backend silently.

---

# Documentation

Update or create frontend documentation containing:

* Frontend architecture
* Route list
* Customer routes
* Admin routes
* Authentication flow
* Cart flow
* Order flow
* Payment history flow
* Razorpay Test Mode limitation
* API service structure
* Environment setup
* Local run commands
* Build commands
* Test commands
* Known limitations
* Part 5 readiness

Preserve all existing documentation.

---

# Before Making Changes

1. Open the project root:

```cmd
cd /d D:\Workspace\professional\Interview_Tasks\Orfus\Payment-Gateway
```

2. Run:

```cmd
git status
```

3. Inspect the complete repository.
4. Read both Part 4 files completely.
5. Inspect the Part 3 backend API documentation.
6. Inspect Swagger and backend route registration.
7. Inspect existing frontend:

   * App Router pages
   * Layout
   * Auth Context
   * Axios client
   * Auth service
   * Login page
   * Register page
   * Dashboard
   * Tailwind setup
   * ESLint setup
   * Existing tests
8. Confirm Part 1, Part 2, and Part 3 remain working.
9. Identify pre-existing dirty files.
10. Summarize the implementation plan.
11. List files expected to be created or modified.
12. Identify API contract conflicts before editing.
13. Do not begin Part 5.

---

# After Implementation

1. List all created files.
2. List all modified files.
3. Run frontend lint.
4. Run frontend tests.
5. Run frontend production build.
6. Start the frontend locally.
7. Start or use the local backend.
8. Verify the home page.
9. Verify registration.
10. Verify login.
11. Verify logout.
12. Verify Customer route protection.
13. Verify Admin route protection.
14. Verify Product listing.
15. Verify Product details.
16. Verify Admin Product CRUD.
17. Verify Cart.
18. Verify Checkout preparation.
19. Verify Order creation.
20. Verify Order history.
21. Verify Order details.
22. Verify Payment history.
23. Verify Admin Payments.
24. Verify Payment Logs.
25. Verify Customer Dashboard.
26. Verify Admin Dashboard.
27. Verify Profile.
28. Verify loading, empty, and error states.
29. Verify mobile responsiveness.
30. Verify browser refresh on protected routes.
31. Verify no sensitive fields are displayed.
32. Run backend regression tests.
33. Verify backend health.
34. Verify Swagger still opens.
35. Verify Docker Compose configuration remains valid.
36. Confirm no full Razorpay integration was implemented.
37. Provide exact Windows CMD commands.
38. List remaining manual steps.
39. Do not start Part 5 automatically.

---

# Suggested Verification Commands

Backend:

```cmd
cd /d D:\Workspace\professional\Interview_Tasks\Orfus\Payment-Gateway\backend

npm run lint
npm test
npm run dev
```

Frontend:

```cmd
cd /d D:\Workspace\professional\Interview_Tasks\Orfus\Payment-Gateway\frontend

npm install
npm run lint
npm test
npm run build
npm run dev
```

If no `npm test` script exists, clearly report it and add a compatible test setup only if it is within the Part 4 scope.

Docker configuration:

```cmd
cd /d D:\Workspace\professional\Interview_Tasks\Orfus\Payment-Gateway

docker compose config
```

Do not use Docker as the only verification method.

Verify local backend and local frontend first.

---

# Manual Browser Verification

Verify:

```text
http://localhost:3000
http://localhost:3000/login
http://localhost:3000/register
http://localhost:3000/dashboard
http://localhost:3000/products
http://localhost:3000/cart
http://localhost:3000/checkout
http://localhost:3000/orders
http://localhost:3000/payments
http://localhost:3000/profile
http://localhost:3000/admin/dashboard
http://localhost:3000/admin/products
http://localhost:3000/admin/orders
http://localhost:3000/admin/payments
http://localhost:3000/admin/payment-logs
```

Backend:

```text
http://localhost:5000/api/v1/health
http://localhost:5000/api-docs/
```

---

# Final Codex Output Format

Provide the final implementation report using these headings:

```text
## Summary

## Repository Inspection

## Implementation Plan

## Files Created

## Files Modified

## Existing Parts 1 to 3 Preservation

## Frontend Architecture

## Routes Created

## Layout and Navigation

## Authentication Integration

## Customer Route Protection

## Admin Route Protection

## API Client

## Frontend Services

## Product Pages

## Admin Product Management

## Cart

## Cart Context

## Checkout Preparation

## Orders

## Admin Orders

## Payment History

## Admin Payments

## Payment Logs

## Customer Dashboard

## Admin Dashboard

## Profile

## Payment Success Foundation

## Payment Failure Foundation

## Forms and Validation

## Search Filtering Sorting and Pagination

## Loading Empty and Error States

## Responsive Design

## Accessibility

## Security Verification

## Frontend Tests

## Backend Regression Check

## Build Results

## Manual Browser Verification

## Docker Configuration Check

## Windows CMD Commands

## Remaining Manual Steps

## Known Limitations

## Part 5 Readiness
```

Do not claim that a page, command, test, build, API call, or browser flow passed unless it was actually verified successfully.

If the backend, PostgreSQL, environment variables, browser verification, or dependencies are unavailable, report the affected check as not configured or not verified.

Do not make unrelated changes.

Do not implement full Razorpay integration.

Do not start Part 5.

Part-5 Razorpay Integration
------------------------------
Read these files completely before making any changes:

1. `docs/Part-5-Razorpay-Integration-Payment-Gateway.md`
2. `docs/Codex-Prompt-Part-5-Razorpay-Integration-Payment-Gateway.md`

Continue from the completed Part 1, Part 2, Part 3, and Part 4 implementation.

Implement ONLY Part 5: Razorpay Test Mode integration, backend payment execution, frontend Razorpay Checkout, payment verification, webhook processing, payment status synchronization, idempotency, logging, Swagger documentation, and tests.

Project root:

```text
D:\Workspace\professional\Interview_Tasks\Orfus\Payment-Gateway
```

Do not create another nested project folder.

---

# Existing Implementation

## Part 1 Completed

* Backend and frontend project foundation
* PostgreSQL and Sequelize configuration
* Registration
* Login
* Logout
* Protected profile
* JWT authentication
* Role authorization
* Swagger
* Winston logging
* Centralized error handling
* Razorpay SDK configuration foundation
* Webhook raw-body route foundation
* Docker foundation
* Backend and frontend tests

## Part 2 Completed

* Roles
* Users
* Products
* Cart Items
* Orders
* Order Items
* Payments
* Payment Logs
* Webhook Events
* Audit fields
* Soft delete
* Constraints
* Indexes
* Sequelize models
* Associations
* Seed products
* Database tests

## Part 3 Completed

Backend APIs for:

* Products
* Cart
* Orders
* Payments
* Payment History
* Payment Logs
* Customer Dashboard
* Admin Dashboard
* Search
* Filtering
* Sorting
* Pagination
* Role and ownership enforcement
* Swagger
* Postman collection
* API tests

## Part 4 Completed

Frontend for:

* Customer Panel
* Admin Panel
* Role-based navigation
* Product listing/details
* Admin product management
* Cart
* Checkout preparation
* Orders
* Payments
* Dashboards
* Profile
* Payment success/failure pages
* Responsive UI
* Error handling
* Frontend tests
* Production build

Preserve all existing working functionality.

---

# Scope

Implement only Razorpay Test Mode payment integration.

Implement:

* Razorpay Order creation
* Payment initiation
* Frontend Razorpay Checkout
* Payment signature verification
* Payment success handling
* Payment failure handling
* Webhook signature verification
* Webhook event persistence
* Payment and Order status synchronization
* Payment logs
* Duplicate prevention
* Idempotency
* Retry-safe processing
* Swagger documentation
* Postman examples
* Backend tests
* Frontend tests
* Manual Razorpay Test Mode verification instructions

Do not implement production payments.

Do not implement refunds unless the Part-5 specification explicitly includes a safe foundation only.

Do not implement settlements, payouts, subscriptions, multi-currency, accounting, or reconciliation beyond the documented Part-5 scope.

---

# Important Restrictions

* Use Razorpay Test Mode only.
* Never expose `RAZORPAY_KEY_SECRET`.
* Never expose `RAZORPAY_WEBHOOK_SECRET`.
* Never log Razorpay secrets.
* Never store card number, CVV, OTP, or banking credentials.
* Never trust payment amount from the frontend.
* Never mark payment successful from frontend state alone.
* Never mark payment successful only because Razorpay Checkout returned success.
* Backend signature verification is mandatory.
* Webhook signature verification is mandatory.
* Do not weaken existing ownership or role authorization.
* Do not recreate correct migrations or models.
* Do not change the database schema unless required for a real implementation issue.
* Explain any schema change before implementing it.
* Do not use `sequelize.sync()`.
* Do not make unrelated frontend or backend redesigns.
* Do not implement Part 6 DevOps automatically.

---

# Payment Architecture

Use this flow:

```text
Customer Checkout
→ Frontend Requests Payment Initialization
→ Backend Loads Trusted Order
→ Backend Validates Ownership and Status
→ Backend Calculates Trusted Amount
→ Backend Creates Razorpay Order
→ Backend Creates/Updates Local Payment Record
→ Frontend Receives Safe Checkout Data
→ Frontend Opens Razorpay Checkout
→ Razorpay Returns Payment Data
→ Frontend Sends Payment Data to Backend
→ Backend Verifies Signature
→ Backend Updates Payment and Order
→ Backend Writes Payment Log
→ Frontend Redirects to Verified Success Page
```

Webhook flow:

```text
Razorpay
→ POST /api/v1/webhooks/razorpay
→ Read Exact Raw Request Body
→ Verify Webhook Signature
→ Persist Webhook Event
→ Prevent Duplicate Processing
→ Process Supported Event
→ Update Payment
→ Update Order
→ Write Payment Log
→ Mark Webhook Processed
```

---

# Backend Architecture

Use:

```text
Route
→ Validation Middleware
→ Authentication/Authorization Middleware
→ Controller
→ Service
→ Razorpay SDK
→ Sequelize Models
→ PostgreSQL
```

Controllers:

* Accept `req`, `res`, `next`
* Use async/await
* Contain only HTTP handling
* Call services
* Pass errors to centralized middleware
* Never contain direct Sequelize queries
* Never contain signature-generation logic inline if a utility exists

Services:

* Contain business logic
* Use Sequelize transactions
* Use Razorpay SDK
* Validate payment state transitions
* Enforce ownership
* Handle duplicate requests
* Write payment logs
* Return safe data only

---

# Environment Variables

Backend:

```env
RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxx
RAZORPAY_KEY_SECRET=xxxxxxxxxxxxxxxx
RAZORPAY_WEBHOOK_SECRET=xxxxxxxxxxxxxxxx
```

Frontend:

```env
NEXT_PUBLIC_RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxx
```

Rules:

* Only Razorpay Key ID may be exposed to frontend.
* Secrets must remain backend-only.
* Do not commit `.env` or `.env.local`.
* Keep `.env.example` with placeholders only.
* Validate missing variables clearly.
* Do not print secrets during startup.

---

# Required Backend Endpoints

Implement or complete:

```text
POST /api/v1/payments/initialize
POST /api/v1/payments/verify
POST /api/v1/payments/failure
GET  /api/v1/payments/:id
GET  /api/v1/payments/history
GET  /api/v1/orders/:orderId/payments
POST /api/v1/webhooks/razorpay
```

Optional Admin read-only endpoint where useful:

```text
GET /api/v1/payments/:id/logs
```

Do not create public payment mutation APIs beyond the documented flow.

---

# Payment Initialization API

Endpoint:

```text
POST /api/v1/payments/initialize
```

Customer authentication required.

Request body:

```json
{
  "orderId": 35
}
```

Do not accept:

* Amount
* Currency
* User ID
* Order number
* Payment status
* Razorpay order ID
* Audit fields

Required flow:

```text
Validate authenticated Customer
→ Load Customer-owned Order
→ Confirm Order exists
→ Confirm Order is not deleted
→ Confirm Order status permits payment
→ Confirm Order payment_status is not already PAID
→ Load trusted total_amount and currency
→ Convert amount to paise
→ Generate receipt/idempotency reference
→ Create Razorpay Order
→ Create or update local Payment row
→ Create Payment Log
→ Return safe Checkout configuration
```

Response should include only safe fields:

```json
{
  "paymentId": 10,
  "applicationOrderId": 35,
  "applicationOrderNumber": "ORD-20260715-00035",
  "razorpayOrderId": "order_xxxxx",
  "razorpayKeyId": "rzp_test_xxxxx",
  "amount": 99900,
  "currency": "INR",
  "name": "Payment Gateway",
  "description": "Payment for order ORD-20260715-00035"
}
```

Do not return the Razorpay Key Secret.

Do not return the Webhook Secret.

---

# Trusted Amount Calculation

Use the backend Order record as the source of truth.

Rules:

* Use `orders.total_amount`.
* Confirm Order Items exist where required.
* Do not use frontend cart total.
* Do not accept frontend amount.
* Convert INR rupees to paise safely.
* Avoid floating-point errors.

Recommended conversion:

```javascript
const amountInPaise = Math.round(Number(order.total_amount) * 100);
```

Validate:

* Amount is positive.
* Currency is supported.
* Amount is within expected range.
* Existing order amount has not been tampered with.

For INR:

```text
₹999.00 = 99900 paise
```

---

# Razorpay Order Creation

Use the configured reusable Razorpay client.

Create Razorpay Order with:

```javascript
{
  amount: amountInPaise,
  currency: "INR",
  receipt: applicationOrderNumber,
  notes: {
    applicationOrderId: String(order.id),
    applicationOrderNumber: order.order_number,
    userId: String(order.user_id)
  }
}
```

Rules:

* Use Test Mode credentials.
* Do not call Razorpay if configuration is missing.
* Do not log secret values.
* Log safe Razorpay order ID and request ID.
* Persist Razorpay order ID.
* Prevent duplicate Razorpay order creation for the same retry.
* Reuse an existing valid pending payment attempt where appropriate.
* Do not create unlimited duplicate pending payment rows.

---

# Idempotency and Duplicate Prevention

Implement a practical idempotency strategy.

Possible strategy:

* One active `CREATED` or `PENDING` payment attempt per application order
* Reuse existing Razorpay order if still valid
* Use unique provider identifiers
* Use transaction and row locking where appropriate
* Reject payment initialization if Order is already paid
* Prevent duplicate verification
* Prevent duplicate webhook processing
* Prevent duplicate Payment Logs for the same state transition where practical

Do not add unnecessary distributed-lock complexity.

Document the chosen strategy.

---

# Local Payment Record

Use the existing `payments` table.

On initialization, store:

* `order_id`
* `user_id`
* `provider = RAZORPAY`
* `razorpay_order_id`
* `amount`
* `currency`
* `status = CREATED` or `PENDING`
* audit fields

Do not store:

* Card number
* CVV
* OTP
* Full banking data
* Secrets

---

# Frontend Razorpay Checkout

Load Razorpay Checkout script safely.

Use:

```text
https://checkout.razorpay.com/v1/checkout.js
```

Implement a reusable loader utility.

Rules:

* Load script only when needed.
* Prevent duplicate script injection.
* Handle script load failure.
* Do not expose secret values.
* Use backend-returned Razorpay Order ID.
* Use backend-returned amount and currency.
* Use public Key ID only.
* Disable payment button while initializing.
* Prevent rapid duplicate clicks.
* Show Test Mode notice.
* Handle modal dismissal.
* Restore UI state after failure.

Checkout options may include:

```javascript
{
  key: razorpayKeyId,
  amount,
  currency,
  name: "Payment Gateway",
  description,
  order_id: razorpayOrderId,
  prefill: {
    name: customerName,
    email: customerEmail
  },
  notes: {
    applicationOrderId,
    applicationOrderNumber
  },
  theme: {}
}
```

Do not send secrets.

Do not trust frontend amount.

---

# Checkout Page Flow

Update:

```text
/checkout
```

Required flow:

```text
Load Customer Cart or Order Context
→ Create Application Order if not already created
→ Request Payment Initialization
→ Open Razorpay Checkout
→ Receive Razorpay Payment Data
→ Call Backend Verification
→ Wait for Verified Backend Response
→ Redirect to Payment Success
```

If application Order already exists, use that Order.

Do not create a new application Order on every retry.

Do not redirect to success before backend verification.

---

# Payment Verification API

Endpoint:

```text
POST /api/v1/payments/verify
```

Customer authentication required.

Request body:

```json
{
  "paymentId": 10,
  "razorpayOrderId": "order_xxxxx",
  "razorpayPaymentId": "pay_xxxxx",
  "razorpaySignature": "signature_value"
}
```

Required flow:

```text
Validate request
→ Load local Payment
→ Validate Customer ownership
→ Match stored Razorpay Order ID
→ Build signature payload
→ Verify HMAC SHA256
→ Reject invalid signature
→ Update Payment in transaction
→ Update Order payment status
→ Update Order status where documented
→ Write Payment Log
→ Return verified result
```

Signature payload:

```text
razorpay_order_id + "|" + razorpay_payment_id
```

Generate expected signature using:

```text
HMAC SHA256 with RAZORPAY_KEY_SECRET
```

Compare signatures safely.

Do not use plain insecure comparison where a timing-safe comparison is practical.

---

# Payment Verification Success

On valid signature:

Payment:

```text
status = CAPTURED or AUTHORIZED
razorpay_payment_id = received payment ID
verified_at = current timestamp
paid_at = current timestamp where appropriate
updated_by = authenticated user ID
```

Order:

```text
payment_status = PAID
```

Order status should change only according to documented business rules.

Possible safe update:

```text
PENDING → CONFIRMED
```

Do not overwrite a later valid Admin status.

Write Payment Log:

```text
event_type = PAYMENT_VERIFIED
previous_status
new_status
request_id
safe metadata
```

Return only verified safe payment details.

---

# Invalid Signature Handling

On invalid signature:

* Do not mark Payment successful.
* Do not mark Order paid.
* Update Payment status only if documented.
* Write a failure Payment Log.
* Return HTTP 400 or 401 according to project conventions.
* Do not expose expected signature.
* Do not expose Key Secret.
* Log safe diagnostic information only.

---

# Payment Failure API

Endpoint:

```text
POST /api/v1/payments/failure
```

Customer authentication required.

Request body may include safe Razorpay failure fields:

```json
{
  "paymentId": 10,
  "razorpayOrderId": "order_xxxxx",
  "errorCode": "BAD_REQUEST_ERROR",
  "errorDescription": "Payment failed",
  "errorReason": "payment_failed",
  "errorSource": "customer",
  "errorStep": "payment_authentication"
}
```

Rules:

* Validate ownership.
* Do not accept arbitrary payment status.
* Do not accept amount.
* Do not trust raw frontend error blindly.
* Store only safe, length-limited fields.
* Update Payment to FAILED where valid.
* Do not mark Order paid.
* Update Order payment status to FAILED only when appropriate.
* Write Payment Log.
* Return safe response.

Modal dismissal without a gateway failure should not necessarily mark Payment failed.

Document the behavior.

---

# Payment Success Page

Update:

```text
/payment-success
```

Requirements:

* Render only after backend verification success.
* Use safe route state or query reference.
* Optionally refetch Payment or Order from backend.
* Display:

  * Order number
  * Payment ID where safe
  * Amount
  * Currency
  * Status
  * Date
* Show Test Mode notice.
* Provide links to Orders and Payments.

Do not mark success from URL parameters alone.

If verification cannot be confirmed, show a pending/error state.

---

# Payment Failure Page

Update:

```text
/payment-failed
```

Requirements:

* Display safe failure message.
* Display order reference where available.
* Provide retry option.
* Provide link to Orders or Products.
* Do not expose raw Razorpay objects.
* Do not create duplicate Order on retry.
* Do not mark payment failed only from query parameters.

---

# Webhook Endpoint

Complete:

```text
POST /api/v1/webhooks/razorpay
```

This route must use the exact raw request body.

Do not parse the webhook body with normal JSON middleware before signature verification.

Verify signature using:

```text
RAZORPAY_WEBHOOK_SECRET
```

Expected signature header:

```text
x-razorpay-signature
```

Required flow:

```text
Receive raw body
→ Read signature header
→ Verify HMAC SHA256
→ Reject invalid signature
→ Parse JSON only after verification
→ Persist Webhook Event
→ Detect duplicate event
→ Process supported event
→ Update Payment/Order
→ Write Payment Log
→ Mark Webhook Event processed
→ Return HTTP 200
```

---

# Supported Webhook Events

Support the documented subset, such as:

```text
payment.authorized
payment.captured
payment.failed
order.paid
```

Handle only supported events.

Unknown events:

* Persist safely if required.
* Mark as ignored.
* Return HTTP 200 after valid signature.
* Do not fail the entire webhook endpoint.

---

# Webhook Event Persistence

Use existing `webhook_events` table.

Store:

* Provider
* Event ID
* Event type
* Entity type
* Razorpay Order ID
* Razorpay Payment ID
* Payload
* Signature
* Signature verification result
* Processing status
* Processed timestamp
* Error message
* Retry count
* Audit/system fields

Do not store the Webhook Secret.

Do not log full sensitive payloads.

---

# Webhook Idempotency

Prevent duplicate webhook processing.

Use:

* Unique `event_id`, or
* Payload hash/idempotency key when event ID is unavailable

Duplicate event behavior:

* Do not repeat state transitions.
* Do not create duplicate logs unnecessarily.
* Return HTTP 200.
* Mark or report as duplicate/ignored.

Use a database transaction.

---

# Webhook Status Synchronization

Examples:

## `payment.authorized`

* Set Payment to AUTHORIZED if current transition is valid.
* Do not move backward from CAPTURED.
* Write log.

## `payment.captured`

* Set Payment to CAPTURED.
* Set Order payment status to PAID.
* Confirm Order where appropriate.
* Write log.

## `payment.failed`

* Set Payment to FAILED if not already successful.
* Do not overwrite CAPTURED with FAILED.
* Update Order payment status only where valid.
* Write log.

## `order.paid`

* Reconcile local Order and Payment.
* Mark Order payment status PAID.
* Do not create duplicate Payment if one already matches.
* Write log.

All transitions must be idempotent.

---

# Payment Status Transition Rules

Define allowed transitions.

Example:

```text
CREATED → PENDING
PENDING → AUTHORIZED
PENDING → FAILED
AUTHORIZED → CAPTURED
AUTHORIZED → FAILED only if valid
CAPTURED → REFUNDED in future phase only
```

Do not allow:

```text
CAPTURED → FAILED
PAID Order → PENDING payment status
```

Use a shared transition helper.

Backend is the final authority.

---

# Order Payment Status Rules

Example:

```text
PENDING → AUTHORIZED
PENDING → PAID
PENDING → FAILED
FAILED → PENDING for a new retry attempt where documented
AUTHORIZED → PAID
PAID remains PAID
```

Do not downgrade PAID.

Multiple failed attempts should not erase a later successful attempt.

Order payment status should reflect the final effective payment state.

---

# Payment Logs

Write logs for:

* Payment initialization
* Razorpay order creation
* Checkout initiated where backend knows
* Verification success
* Verification failure
* Payment failure
* Webhook received
* Webhook verified
* Webhook duplicate
* Payment authorized
* Payment captured
* Payment failed
* Order marked paid
* Invalid state transition
* Razorpay API error

Store safe metadata only.

Do not store:

* Secrets
* JWT tokens
* Card data
* CVV
* Full raw sensitive headers

---

# Logging

Use Winston.

Include:

* Request ID
* User ID
* Application Order ID
* Local Payment ID
* Razorpay Order ID
* Razorpay Payment ID where safe
* Event type
* Status transition

Do not log:

* Key Secret
* Webhook Secret
* Full signature
* Card information
* Complete sensitive payloads

---

# Error Handling

Handle:

* Missing Razorpay configuration
* Invalid Order
* Unauthorized Order access
* Already paid Order
* Duplicate initialization
* Razorpay API failure
* Invalid payment signature
* Payment ID mismatch
* Razorpay Order ID mismatch
* Invalid status transition
* Webhook signature failure
* Duplicate webhook event
* Unsupported webhook event
* Database transaction failure
* Network failure
* Unexpected internal error

Use centralized error middleware.

Return request ID.

Do not expose stack traces in production.

---

# Frontend Error Handling

Every async payment operation must use:

```text
try
catch
finally
```

Requirements:

* Disable payment button while processing.
* Re-enable after failure.
* Prevent duplicate submissions.
* Normalize backend errors.
* Preserve request ID.
* Show useful message.
* Handle Checkout script failure.
* Handle user dismissal.
* Handle verification failure.
* Handle backend timeout.
* Do not redirect to success on failure.
* Do not render raw Razorpay error objects.

---

# Swagger

Document:

```text
POST /payments/initialize
POST /payments/verify
POST /payments/failure
POST /webhooks/razorpay
GET  /payments/:id
GET  /payments/history
GET  /orders/:orderId/payments
```

Include:

* Authentication requirements
* Request schemas
* Response schemas
* Validation errors
* Authorization errors
* Conflict errors
* Signature verification notes
* Webhook raw-body notes
* Test Mode warning
* Example payloads with fake IDs only

Do not put real secrets in Swagger examples.

---

# Postman

Update or add Part-5 Postman collection.

Include:

* Login
* Payment initialize
* Payment verify with placeholder values
* Payment failure
* Payment history
* Payment detail
* Webhook placeholder/manual testing instructions

Do not include real Key Secret or Webhook Secret.

Clearly state that signature values must come from Razorpay Test Mode.

---

# Backend Tests

Mock Razorpay SDK and external calls.

Do not call real Razorpay APIs in automated tests.

Test:

## Initialization

* Customer can initialize payment for own Order.
* Customer cannot initialize another user’s Order.
* Already paid Order rejected.
* Trusted backend amount used.
* Frontend amount ignored/rejected.
* Razorpay Order created with paise.
* Local Payment created.
* Duplicate initialization handled.
* Missing Razorpay config handled.

## Verification

* Valid signature accepted.
* Invalid signature rejected.
* Order ID mismatch rejected.
* Payment ID mismatch rejected.
* Payment updated.
* Order payment status updated.
* Payment Log created.
* Duplicate verification is idempotent.
* Successful Payment not downgraded.

## Failure

* Valid failure updates Payment.
* Ownership enforced.
* Safe error fields stored.
* Order not marked paid.
* Successful Payment not overwritten by failure.

## Webhooks

* Valid webhook signature accepted.
* Invalid signature rejected.
* Duplicate event ignored.
* `payment.authorized` processed.
* `payment.captured` processed.
* `payment.failed` processed safely.
* `order.paid` processed.
* Unsupported event ignored safely.
* CAPTURED payment not downgraded.
* Payment Log created.
* Webhook Event status updated.

## Regression

All previous backend tests must continue to pass.

---

# Frontend Tests

Mock backend and Razorpay Checkout.

Test:

* Checkout script loads.
* Script load failure shown.
* Initialize API called once.
* Payment button disabled during request.
* Razorpay Checkout options use backend amount/order ID.
* Public Key ID only.
* Success handler calls verification API.
* Invalid verification does not redirect to success.
* Valid verification redirects to success.
* Failure callback calls safe failure API.
* Modal dismissal handled.
* Duplicate click prevented.
* Customer-only payment action.
* Admin does not see Pay button.
* Payment success page refetches or validates safe data.
* Payment failure page displays safe error.
* No secrets rendered.

All previous frontend tests must continue to pass.

---

# Manual Razorpay Test Mode Verification

After implementation, provide exact manual steps.

Verify with a Razorpay Test Account.

Required manual flow:

```text
Customer login
→ Add Product to Cart
→ Checkout
→ Create Application Order
→ Click Pay Now
→ Razorpay Test Checkout opens
→ Complete test payment
→ Backend verifies signature
→ Payment becomes CAPTURED/PAID
→ Order payment status becomes PAID
→ Success page displays verified result
→ Payment history shows payment
→ Admin payment list shows payment
→ Payment logs show transitions
```

Also test:

* Failed payment
* User closes Checkout
* Invalid signature through API test
* Duplicate verification request
* Duplicate webhook
* Customer attempting another Customer’s Order
* Already-paid Order retry
* Backend unavailable
* Razorpay script unavailable

---

# Test Mode Notice

Display visible notice:

```text
Razorpay Test Mode — No real money will be charged.
```

Show it on:

* Checkout
* Payment success
* Payment failure
* Payment details where appropriate

---

# Security Verification

Confirm:

* Key Secret absent from frontend source and build output
* Webhook Secret absent from frontend source and build output
* No secrets in Git-tracked files
* No secrets in logs
* No card information stored
* Amount calculated from backend Order
* Signature verified backend-side
* Webhook verified with raw body
* Ownership enforced
* Duplicate processing prevented
* Success not trusted from frontend only
* Payment statuses cannot be downgraded incorrectly

Search frontend and repository for:

```text
RAZORPAY_KEY_SECRET
RAZORPAY_WEBHOOK_SECRET
```

Only backend environment/configuration references should exist.

---

# Before Making Changes

1. Open project root:

```cmd
cd /d D:\Workspace\professional\Interview_Tasks\Orfus\Payment-Gateway
```

2. Run:

```cmd
git status
```

3. Inspect all dirty files.
4. Read both Part-5 documents completely.
5. Inspect:

   * Razorpay config
   * Payment model
   * Order model
   * Payment Log model
   * Webhook Event model
   * Payment services/routes/controllers
   * Webhook route/raw-body handling
   * Checkout page
   * Payment success/failure pages
   * Axios client
   * Auth and Cart contexts
   * Swagger
   * Existing tests
6. Confirm Part 1–4 tests pass.
7. Confirm frontend build passes.
8. Identify API/schema conflicts.
9. Summarize implementation plan.
10. List expected files to create or modify.
11. Explain any schema change before applying it.
12. Do not start Part 6.

---

# After Implementation

1. List all created files.
2. List all modified files.
3. Explain Razorpay initialization flow.
4. Explain trusted amount calculation.
5. Explain idempotency strategy.
6. Explain signature verification.
7. Explain webhook verification.
8. Explain payment status transitions.
9. Explain Order payment status transitions.
10. Run backend lint.
11. Run backend tests.
12. Run migrations.
13. Check migration status.
14. Run frontend lint.
15. Run frontend tests.
16. Run frontend build.
17. Start backend and frontend locally.
18. Verify health.
19. Verify Swagger.
20. Verify Customer checkout.
21. Verify Razorpay Test Checkout.
22. Verify successful payment.
23. Verify failed payment.
24. Verify payment history.
25. Verify Admin payments.
26. Verify payment logs.
27. Verify webhook endpoint.
28. Verify duplicate prevention.
29. Verify ownership.
30. Verify no secrets are exposed.
31. Verify Docker Compose config remains valid.
32. Provide exact Windows CMD commands.
33. List manual Razorpay dashboard/webhook setup.
34. Do not start Part 6.

---

# Suggested Commands

Backend:

```cmd
cd /d D:\Workspace\professional\Interview_Tasks\Orfus\Payment-Gateway\backend

npm install
npm run lint
npm run db:migrate
npm run db:migrate:status
npm test
npm run dev
```

Frontend:

```cmd
cd /d D:\Workspace\professional\Interview_Tasks\Orfus\Payment-Gateway\frontend

npm install
npm run lint
npm test
npm run build
npm run dev
```

Root:

```cmd
cd /d D:\Workspace\professional\Interview_Tasks\Orfus\Payment-Gateway

docker compose config
```

Do not use Docker as the only verification method.

Verify local PostgreSQL, backend, and frontend first.

---

# Manual URLs

Frontend:

```text
http://localhost:3000/login
http://localhost:3000/products
http://localhost:3000/cart
http://localhost:3000/checkout
http://localhost:3000/orders
http://localhost:3000/payments
http://localhost:3000/payment-success
http://localhost:3000/payment-failed
http://localhost:3000/admin/payments
http://localhost:3000/admin/payment-logs
```

Backend:

```text
http://localhost:5000/api/v1/health
http://localhost:5000/api-docs/
```

Webhook:

```text
POST http://localhost:5000/api/v1/webhooks/razorpay
```

---

# Final Codex Output Format

Provide:

```text
## Summary

## Repository Inspection

## Implementation Plan

## Files Created

## Files Modified

## Existing Parts 1 to 4 Preservation

## Razorpay Configuration

## Payment Initialization

## Trusted Amount Calculation

## Razorpay Order Creation

## Idempotency Strategy

## Frontend Checkout Integration

## Payment Verification

## Payment Failure Handling

## Webhook Signature Verification

## Webhook Event Processing

## Payment Status Transitions

## Order Payment Status Transitions

## Payment Logs

## Error Handling

## Logging

## Swagger

## Postman

## Backend Tests

## Frontend Tests

## Migration Results

## Build Results

## Manual Razorpay Test Mode Verification

## Security Verification

## Docker Configuration Check

## Windows CMD Commands

## Remaining Manual Steps

## Known Limitations

## Part 6 Readiness
```

Do not claim that a Razorpay payment, webhook, test, build, API call, or browser flow passed unless it was actually verified.

If Razorpay credentials are unavailable, report live Test Mode verification as not configured or not verified.

Do not expose secrets.

Do not make unrelated changes.

Do not implement Part 6.

Do not start production deployment.


Part-6 → DevOps
---------------
Read these files completely before making any changes:

1. `docs/Part-6-DevOps-Payment-Gateway.md`
2. `docs/Codex-Prompt-Part-6-DevOps-Payment-Gateway.md`

Continue from the completed Part 1, Part 2, Part 3, Part 4, and Part 5 implementation.

Implement ONLY Part 6: DevOps, containerization hardening, CI/CD, security scanning, deployment foundation, monitoring, backups, rollback, and operational documentation.

Project root:

```text
D:\Workspace\professional\Interview_Tasks\Orfus\Payment-Gateway
```

Do not create another nested project folder.

---

# Existing Implementation

## Part 1 Completed

* Backend and frontend foundation
* PostgreSQL and Sequelize configuration
* Authentication
* JWT
* Role authorization
* Logging
* Swagger
* Docker foundation
* Health API
* Tests

## Part 2 Completed

* Complete database schema
* Audit fields
* Soft delete
* Models
* Associations
* Migrations
* Constraints
* Indexes
* Seed data
* Database tests

## Part 3 Completed

* Product APIs
* Cart APIs
* Order APIs
* Payment read APIs
* Payment logs
* Dashboards
* Search
* Filtering
* Sorting
* Pagination
* Ownership and authorization
* Swagger and Postman
* API tests

## Part 4 Completed

* Customer Panel
* Admin Panel
* Role-based layouts
* Product pages
* Cart
* Checkout UI
* Orders
* Payments
* Dashboards
* Profile
* Responsive frontend
* Frontend tests
* Production build

## Part 5 Completed

* Razorpay Test Mode configuration
* Razorpay Order creation
* Checkout.js integration
* Backend signature verification
* Payment failure handling
* Webhook verification
* Idempotency
* Payment logs
* Payment and Order synchronization
* Tests
* Swagger and Postman updates

Preserve all working functionality from Parts 1 to 5.

---

# Scope

Implement only DevOps and operational improvements for:

* Backend production Docker image
* Frontend production Docker image
* PostgreSQL service configuration
* Docker Compose development setup
* Docker Compose production foundation
* Environment variable management
* Health checks
* Database readiness
* Database migration strategy
* CI pipeline
* Security scanning
* Dependency scanning
* Secret scanning
* CodeQL
* Trivy
* Docker image validation
* Optional container registry publishing
* Deployment foundation
* Reverse proxy foundation
* HTTPS guidance
* Logging
* Monitoring
* Backups
* Restore procedure
* Rollback
* Graceful shutdown
* Smoke tests
* Runbook
* Documentation

Do not implement live production deployment unless valid credentials and deployment targets are already configured.

Do not fake a successful deployment.

---

# Important Restrictions

* Do not modify business logic.
* Do not modify API contracts.
* Do not modify database schema unless required for a real DevOps-related issue.
* Explain any schema change before applying it.
* Do not remove working Docker support.
* Do not hardcode secrets.
* Do not commit real `.env` files.
* Do not put Razorpay secrets in frontend images.
* Do not expose database passwords.
* Do not use Razorpay Live Mode.
* Do not run real payments in CI.
* Do not run destructive database commands automatically.
* Do not use `sequelize.sync()`.
* Do not claim cloud deployment passed unless it was actually performed.
* Do not create fake cloud credentials.
* Do not start another application phase.
* Do not introduce unnecessary enterprise complexity.

---

# Primary Deployment Strategy

Use this as the primary deployment foundation:

```text
Linux VM
→ Docker Engine
→ Docker Compose
→ Reverse Proxy
→ Backend Container
→ Frontend Container
→ PostgreSQL Container or Managed PostgreSQL
```

For interview readiness, support:

```text
Docker Compose on a Linux VM
```

Alternative deployment options may be documented:

* Azure App Service / Container Apps
* AWS ECS or EC2
* Render
* Railway
* DigitalOcean
* Other managed container platforms

Do not implement multiple deployment systems unless required.

Keep one primary working path and document alternatives.

---

# Docker Architecture

Expected services:

```text
postgres
backend
frontend
reverse-proxy
```

The reverse proxy may be included only if implemented safely.

Expected flow:

```text
User
→ HTTPS Reverse Proxy
→ Frontend
→ Backend API
→ PostgreSQL
→ Razorpay Test Mode
```

Razorpay webhook flow:

```text
Razorpay
→ Public HTTPS URL
→ Reverse Proxy
→ Backend Webhook Endpoint
→ Signature Verification
→ PostgreSQL
```

---

# Backend Dockerfile

Inspect the existing backend Dockerfile.

Improve it for production using a multi-stage build where useful.

Requirements:

* Use a supported Node.js LTS image.
* Do not use Node 26 unless project compatibility is verified.
* Prefer a stable LTS version such as Node 22 if compatible.
* Use a small official image.
* Set `NODE_ENV=production`.
* Install only production dependencies in the final image.
* Use `npm ci`.
* Use lockfile.
* Run as a non-root user.
* Copy only required files.
* Exclude logs, tests, local `.env`, and unnecessary files.
* Add a health check where appropriate.
* Expose only the required port.
* Support graceful shutdown.
* Avoid storing secrets in image layers.
* Avoid running migrations automatically on every application process start unless explicitly designed.
* Do not run seeders in production automatically.

Expected backend command:

```text
node src/server.js
```

or the existing production start command.

---

# Frontend Dockerfile

Inspect the existing frontend Dockerfile.

Improve it for Next.js production.

Requirements:

* Use multi-stage build.
* Use a supported Node.js LTS image.
* Use `npm ci`.
* Run `next build`.
* Use Next.js standalone output if appropriate.
* Copy only required runtime files.
* Run as non-root.
* Use production environment.
* Exclude source secrets.
* Do not put Razorpay Key Secret or Webhook Secret in build arguments.
* Only public environment variables may be exposed.
* Add health check where appropriate.
* Expose only the frontend port.
* Preserve source maps according to documented policy.
* Avoid shipping development dependencies where possible.

If using standalone output, verify `next.config.mjs` supports it.

---

# Docker Ignore

Create or improve:

```text
backend/.dockerignore
frontend/.dockerignore
```

Exclude:

```text
node_modules
npm-debug.log
.env
.env.*
logs
coverage
.git
.github
tests
.next
Dockerfile*
README files where unnecessary
```

Do not accidentally exclude required production files.

Preserve `.env.example`.

---

# Docker Compose Development

Create or improve the existing:

```text
docker-compose.yml
```

Services:

```text
postgres
backend
frontend
```

Requirements:

* Shared network
* Named PostgreSQL volume
* Health checks
* Dependency conditions where supported
* Correct service hostnames
* Backend connects to `postgres`, not `localhost`
* Frontend connects to backend correctly
* Environment variables loaded safely
* Restart policy appropriate for development
* Port mappings documented
* No real secrets
* Preserve local development outside Docker
* Avoid mounting `node_modules` incorrectly
* Avoid hiding container dependencies through host mounts

Do not use Docker as the only local development path.

---

# Production Compose Foundation

Create or improve:

```text
docker-compose.prod.yml
```

Expected services:

```text
postgres
backend
frontend
reverse-proxy
```

Requirements:

* Production images
* No source-code bind mounts
* Restart policies
* Health checks
* Read-only filesystem where practical
* `no-new-privileges` where practical
* Named volumes
* Environment file references
* Resource limits where supported
* Log rotation
* Internal network separation where useful
* Only reverse proxy ports exposed publicly
* PostgreSQL not publicly exposed
* Backend optionally internal-only
* Secrets not committed
* Clear deployment documentation

Do not include fake secrets.

Use placeholders only in example files.

---

# Environment Management

Create or improve:

```text
.env.example
backend/.env.example
frontend/.env.example
.env.production.example
```

Document required variables.

Root variables may include:

```env
POSTGRES_DB=payment_gateway_demo
POSTGRES_USER=gopal
POSTGRES_PASSWORD=replace_with_secure_password

BACKEND_PORT=5000
FRONTEND_PORT=3000

BACKEND_IMAGE=payment-gateway-backend
FRONTEND_IMAGE=payment-gateway-frontend
IMAGE_TAG=latest
```

Backend variables:

```env
NODE_ENV=production
PORT=5000

DB_HOST=postgres
DB_PORT=5432
DB_NAME=payment_gateway_demo
DB_USER=gopal
DB_PASSWORD=replace_with_secure_password

JWT_SECRET=replace_with_long_random_secret
JWT_EXPIRES_IN=1d

RAZORPAY_KEY_ID=replace_with_test_key_id
RAZORPAY_KEY_SECRET=replace_with_test_key_secret
RAZORPAY_WEBHOOK_SECRET=replace_with_webhook_secret

CORS_ORIGIN=https://example.com
LOG_LEVEL=info
```

Frontend variables:

```env
NEXT_PUBLIC_API_BASE_URL=https://example.com/api/v1
NEXT_PUBLIC_RAZORPAY_KEY_ID=replace_with_test_key_id
```

Rules:

* Do not commit real `.env`.
* Do not expose backend secrets in frontend variables.
* Do not print environment values in logs.
* Fail clearly when required production variables are missing.
* Use placeholders only in examples.

---

# Database Readiness

Implement a safe database readiness strategy.

Possible approach:

```text
Backend startup
→ Retry PostgreSQL connection
→ Start HTTP server only after connection succeeds
```

or use an explicit startup script.

Requirements:

* Limited retry count
* Exponential or fixed retry delay
* Clear safe logs
* Do not log passwords
* Fail after configured retries
* Do not loop forever silently
* Preserve local development behavior

Do not replace migrations with automatic schema synchronization.

---

# Migration Strategy

Production migration strategy must be explicit.

Recommended:

```text
Deployment
→ Start PostgreSQL
→ Run one migration job
→ Confirm success
→ Start backend
→ Start frontend
```

Implement or document commands such as:

```cmd
docker compose -f docker-compose.prod.yml run --rm backend npm run db:migrate
```

Requirements:

* Migrations run once.
* Migrations must complete before new application version receives traffic.
* Do not run seeders automatically in production.
* Development seeders remain optional.
* Migration failure should stop deployment.
* Document rollback limitations.
* Avoid concurrent migration execution by multiple backend replicas.

Do not automatically undo migrations on deployment failure.

---

# Seed Strategy

Development:

* Admin role
* Customer role
* Demo admin
* Sample products

Production:

* Roles may be seeded through an explicit controlled step.
* Demo users must not be created automatically.
* Sample products must not be inserted automatically.
* Default Admin credentials must not be used in production.

Document production-safe bootstrap steps.

---

# Health Checks

Backend health endpoint:

```text
GET /api/v1/health
```

Should report:

* Application status
* Environment
* Timestamp
* Database connectivity

Do not expose:

* Passwords
* Secrets
* Full connection strings
* Internal stack traces

Frontend health check may use:

```text
/
```

or a lightweight route if available.

PostgreSQL health check:

```text
pg_isready
```

Docker Compose health checks must use these.

---

# Graceful Shutdown

Review backend server shutdown behavior.

Handle:

```text
SIGTERM
SIGINT
```

Required flow:

```text
Stop accepting new requests
→ Close HTTP server
→ Close Sequelize connection
→ Flush safe logs
→ Exit process
```

Requirements:

* Log shutdown start and completion.
* Use a shutdown timeout.
* Avoid hanging indefinitely.
* Avoid abruptly terminating database transactions where possible.
* Do not expose secrets.

Add tests or documented manual verification where practical.

---

# CI/CD Platform

Use GitHub Actions.

Create workflows under:

```text
.github/workflows/
```

Recommended workflows:

```text
ci.yml
security.yml
docker-build.yml
deploy-staging.yml
```

Avoid excessive workflow fragmentation if a smaller set is clearer.

---

# CI Workflow

Trigger on:

```text
push
pull_request
```

Target branches:

```text
main
develop
```

Adapt to actual repository strategy.

CI jobs should include:

## Backend

* Checkout
* Setup Node LTS
* Cache npm dependencies
* `npm ci`
* Backend lint
* Backend tests
* Optional test PostgreSQL service
* Run migrations against test database
* Do not call Razorpay APIs

## Frontend

* `npm ci`
* Frontend lint
* Frontend tests
* Frontend production build

## Docker

* Validate Docker Compose
* Build backend image
* Build frontend image
* Optionally run basic container smoke checks

Requirements:

* Fail fast on errors.
* Do not use real secrets.
* Use fake or mocked Razorpay values for tests where required.
* Do not run live payments.
* Do not deploy from pull requests.
* Pin action major versions.
* Use minimal permissions.

---

# PostgreSQL CI Service

Use a GitHub Actions PostgreSQL service where backend tests require a real database.

Example values must be test-only.

Use:

```text
POSTGRES_DB=payment_gateway_test
POSTGRES_USER=test_user
POSTGRES_PASSWORD=test_password
```

Requirements:

* Wait for database readiness.
* Run all migrations.
* Run tests.
* Clean up automatically after the job.
* Do not use production credentials.
* Do not use the development database.

---

# Frontend Build

The CI workflow must run:

```cmd
npm run build
```

The build must be considered passed only when the command exits successfully.

Do not rely on a time-limited manual observation.

Use supported Node LTS.

If Next.js build hangs under one Node version, investigate compatibility and pin a working LTS version.

Document the selected Node version in:

```text
.nvmrc
```

or:

```text
.node-version
```

and `package.json` engines where appropriate.

---

# CodeQL

Create CodeQL analysis for JavaScript/TypeScript.

Requirements:

* Run on push, pull request, and scheduled basis where appropriate.
* Use minimal permissions.
* Analyze backend and frontend.
* Do not block development unnecessarily for unsupported generated files.
* Exclude dependencies and build outputs naturally.
* Document where results appear.

---

# Dependency Scanning

Use:

```text
npm audit
Dependabot
```

Create:

```text
.github/dependabot.yml
```

Configure for:

* Backend npm
* Frontend npm
* GitHub Actions
* Docker where supported

Do not automatically run `npm audit fix --force`.

Document:

* Vulnerability severity
* Direct vs transitive dependency
* Breaking-change review
* Upgrade testing requirement

---

# Trivy Scanning

Add Trivy scans for:

* Repository filesystem
* Backend Docker image
* Frontend Docker image

Requirements:

* Scan HIGH and CRITICAL vulnerabilities.
* Ignore unfixed issues only when documented.
* Produce readable output.
* Optionally upload SARIF to GitHub Security.
* Do not fail the pipeline on every low-severity issue.
* Document remediation process.

---

# Secret Scanning

Implement secret scanning.

Use:

* GitHub secret scanning where available
* Gitleaks or TruffleHog in CI if appropriate

Scan:

* Repository
* Git history where practical
* Docker build context

Exclude safe placeholder examples only through precise rules.

Do not suppress real findings broadly.

Confirm that:

```text
RAZORPAY_KEY_SECRET
RAZORPAY_WEBHOOK_SECRET
JWT_SECRET
DB_PASSWORD
```

real values are not committed.

---

# Docker Build Workflow

Create a workflow to build images.

Tags may include:

```text
latest
branch name
commit SHA
semantic version tag
```

Prefer immutable tags:

```text
sha-<commit>
```

Requirements:

* Build backend and frontend separately.
* Use Docker Buildx.
* Use layer caching where appropriate.
* Do not publish images unless registry credentials are configured.
* Do not claim publishing succeeded when credentials are absent.
* Validate images even if not pushed.

Optional registries:

* GitHub Container Registry
* Docker Hub
* Azure Container Registry
* AWS ECR

Choose one only if configured.

---

# Image Publishing

If GitHub Container Registry is used:

```text
ghcr.io/<owner>/payment-gateway-backend
ghcr.io/<owner>/payment-gateway-frontend
```

Requirements:

* Push only from trusted branches/tags.
* Use `GITHUB_TOKEN` with minimal permissions.
* Do not push from pull requests.
* Use immutable tags.
* Document image pull commands.

If registry setup is not available, keep build-only workflow and report publishing as not configured.

---

# Staging Deployment Workflow

Create a protected staging deployment workflow only if it can remain safe without fake credentials.

Possible trigger:

```text
workflow_dispatch
```

or:

```text
push to main
```

Requirements:

* Use GitHub Environment protection.
* Use environment secrets.
* Do not expose secrets in logs.
* Run migrations before application restart.
* Run smoke tests after deployment.
* Roll back application image if smoke tests fail.
* Do not automatically roll back database migrations.
* Report deployment as not configured if target details are absent.

Do not create a fake successful SSH deployment.

---

# Linux VM Deployment Foundation

Document a practical deployment to Ubuntu Linux VM.

Required prerequisites:

```text
Ubuntu
Docker Engine
Docker Compose plugin
Git or registry access
Domain name
HTTPS certificate
Firewall
```

Document steps:

1. Provision server.
2. Create deployment user.
3. Install Docker.
4. Configure firewall.
5. Copy environment file securely.
6. Pull repository or images.
7. Start PostgreSQL.
8. Run migrations.
9. Start backend/frontend/reverse proxy.
10. Verify health.
11. Configure HTTPS.
12. Configure Razorpay webhook URL.
13. Run smoke tests.
14. Monitor logs.

Do not include real IPs, passwords, or tokens.

---

# Reverse Proxy

Use Nginx or Caddy if included.

Preferred behavior:

```text
https://example.com
→ frontend

https://example.com/api/
→ backend

https://example.com/api-docs/
→ backend Swagger

https://example.com/api/v1/webhooks/razorpay
→ backend raw webhook route
```

Requirements:

* Preserve raw request body behavior for webhook route.
* Set proxy headers.
* Support HTTPS.
* Set reasonable timeouts.
* Limit request body size.
* Do not cache authenticated API responses.
* Do not expose PostgreSQL.
* Add security headers where appropriate.
* Preserve client IP safely.
* Document CORS changes for same-domain deployment.

Do not break Razorpay webhook signature verification.

---

# HTTPS

Document:

* Domain DNS
* Let’s Encrypt
* Certificate renewal
* HTTP-to-HTTPS redirect
* TLS configuration
* Secure cookies if authentication later moves to cookies

Do not claim HTTPS configured unless tested.

---

# CORS

Production CORS should allow only approved frontend origins.

Example:

```text
https://payment.example.com
```

Requirements:

* No wildcard origin with credentials.
* Environment-based origin list.
* Development localhost allowed only in development.
* Webhook route should not depend on browser CORS.

---

# Logging

Backend already uses Winston.

Improve operational logging where needed.

Requirements:

* Structured JSON logs in production
* Human-readable logs in development
* Request IDs
* User ID where available
* Order ID
* Payment ID
* Razorpay IDs where safe
* Error category
* HTTP method/path/status
* Duration where practical

Do not log:

* Passwords
* JWT tokens
* Razorpay secrets
* Full signatures
* Card data
* Full sensitive webhook payloads

Support stdout logging for containers.

Do not depend only on files inside containers.

---

# Docker Log Rotation

Configure log rotation in production Compose.

Example:

```yaml
logging:
  driver: json-file
  options:
    max-size: "10m"
    max-file: "5"
```

Use reasonable values.

Avoid unlimited log growth.

---

# Monitoring

Document or add foundation for:

* Application health
* Container health
* CPU
* Memory
* Disk
* PostgreSQL connections
* HTTP error rate
* API latency
* Failed payment rate
* Webhook failures
* Payment initialization failures

Possible options:

* Docker health checks
* Uptime monitoring
* Prometheus/Grafana
* Azure Monitor
* AWS CloudWatch
* Sentry

Do not add a heavy monitoring stack unless justified.

For this project, implement lightweight health and log-based monitoring guidance.

---

# Alerting

Document alerts for:

* Backend unhealthy
* Frontend unavailable
* Database unavailable
* Disk space low
* High HTTP 5xx rate
* Repeated webhook verification failures
* Payment failure spike
* Migration failure
* Backup failure
* Certificate expiration

Do not claim alerts are active unless configured.

---

# PostgreSQL Backups

Implement or document backup scripts under:

```text
scripts/
```

Example files:

```text
scripts/backup-postgres.sh
scripts/restore-postgres.sh
scripts/verify-backup.sh
```

Optional Windows equivalents may be included where useful.

Backup requirements:

* Use `pg_dump`
* Timestamped backup files
* Configurable retention
* Compression
* Exit on errors
* Do not print passwords
* Support Docker Compose PostgreSQL
* Document managed PostgreSQL alternatives
* Protect backup files
* Do not commit backups

Add backup folders to `.gitignore`.

---

# Restore Procedure

Document:

1. Stop application writes.
2. Verify backup file.
3. Create restore database.
4. Restore using `pg_restore` or `psql`.
5. Run verification queries.
6. Restart backend.
7. Verify health.
8. Verify sample data.
9. Document recovery result.

Do not overwrite production automatically.

Require explicit database target.

---

# Backup Verification

A backup is not valid until restore is tested.

Provide a safe local verification process using a disposable database.

Example:

```text
payment_gateway_restore_test
```

Do not disturb the main development database.

---

# Rollback Strategy

Application rollback:

```text
Current image tag
→ Previous known-good image tag
→ Restart services
→ Run health checks
```

Requirements:

* Use immutable image tags.
* Preserve previous image.
* Document rollback command.
* Run smoke tests.
* Do not automatically undo migrations.
* Document backward-compatible migration strategy.

Database rollback:

* Prefer forward-fix migrations.
* Use migration undo only after review.
* Take backup before destructive changes.
* Do not run automatic rollback in CI/CD.

---

# Deployment Smoke Tests

Create or improve a script such as:

```text
scripts/smoke-test.sh
```

Check:

```text
Frontend home returns 200
Backend health returns 200
Swagger returns 200 where enabled
Public products returns 200
Unauthorized profile returns 401
```

Do not perform real Razorpay payments.

Optionally verify:

* Login with a controlled test account in staging
* Payment initialization only with mocked or isolated test data

Avoid destructive data changes.

---

# Operations Runbook

Create:

```text
docs/operations-runbook.md
```

Include:

* Start services
* Stop services
* Restart services
* View logs
* Check health
* Run migrations
* Run development seeders
* Backup database
* Restore database
* Rotate secrets
* Renew certificates
* Roll back application
* Troubleshoot database connection
* Troubleshoot frontend API URL
* Troubleshoot Razorpay webhook
* Troubleshoot payment initialization
* Check disk space
* Check container status

Use exact commands.

---

# Secret Rotation

Document safe rotation for:

* JWT secret
* Database password
* Razorpay Key Secret
* Razorpay Webhook Secret

Explain impact.

Examples:

## JWT Secret

* Existing tokens become invalid.
* Users must log in again.

## Database Password

* Update database role.
* Update environment secret.
* Restart backend.
* Verify connection.

## Razorpay Key Secret

* Update backend secret.
* Restart backend.
* Verify Test Mode order creation.

## Webhook Secret

* Update Razorpay dashboard.
* Update backend secret.
* Restart backend.
* Send test webhook.

Do not expose actual values.

---

# Swagger in Production

Decide and document one:

* Swagger enabled only in non-production
* Swagger protected
* Swagger available internally

Do not expose sensitive examples.

Do not include secrets.

---

# Security Hardening

Review:

* Helmet
* CORS
* Rate limiting
* Request body limits
* Non-root containers
* Secret management
* Database network exposure
* HTTPS
* Dependency vulnerabilities
* Image vulnerabilities
* Log sanitization
* Webhook route
* JWT expiration
* Error responses

Do not change working application behavior unnecessarily.

Add rate limiting only if implemented consistently and tested.

At minimum consider:

* Login endpoint
* Payment initialization
* Payment verification
* Webhook endpoint where appropriate

Do not block valid Razorpay webhook retries through overly strict limits.

---

# CI Tests Must Include

Backend:

```text
npm run lint
npm test
npm run db:migrate
npm run db:migrate:status
```

Frontend:

```text
npm run lint
npm test
npm run build
```

Root:

```text
docker compose config
docker build backend
docker build frontend
```

Do not mark CI successful when a command times out.

---

# Required Files

Create or improve where appropriate:

```text
.github/workflows/ci.yml
.github/workflows/security.yml
.github/workflows/docker-build.yml
.github/workflows/deploy-staging.yml

.github/dependabot.yml

docker-compose.yml
docker-compose.prod.yml

backend/Dockerfile
frontend/Dockerfile
backend/.dockerignore
frontend/.dockerignore

scripts/backup-postgres.sh
scripts/restore-postgres.sh
scripts/verify-backup.sh
scripts/smoke-test.sh

docs/deployment.md
docs/operations-runbook.md
docs/backup-and-restore.md
docs/security-and-secrets.md
```

Do not create empty placeholder files.

If a workflow cannot work without user configuration, implement the safe structure and document required secrets.

---

# Tests and Verification

Add tests or script verification for:

* Backend Docker image builds
* Frontend Docker image builds
* Container runs as non-root where practical
* Health checks work
* Compose config valid
* Environment placeholders safe
* CI YAML valid
* Backup script syntax valid
* Smoke test script syntax valid
* Secret scan finds no real secrets
* Previous backend tests still pass
* Previous frontend tests still pass
* Frontend production build passes

Do not run destructive restore against the main database.

---

# Manual Verification

Local verification order:

```text
1. Local PostgreSQL
2. Backend local
3. Frontend local
4. Automated tests
5. Production builds
6. Docker image builds
7. Docker Compose development
8. Production Compose validation
9. Smoke tests
10. Backup and disposable restore test
```

Do not use Docker as the only verification path.

---

# Before Making Changes

1. Open:

```cmd
cd /d D:\Workspace\professional\Interview_Tasks\Orfus\Payment-Gateway
```

2. Run:

```cmd
git status
```

3. Inspect all current dirty files.
4. Read both Part-6 documents completely.
5. Inspect:

   * Existing Dockerfiles
   * Docker Compose
   * `.dockerignore`
   * `.gitignore`
   * Package scripts
   * Backend startup
   * Database connection handling
   * Graceful shutdown
   * Health endpoint
   * Environment files
   * GitHub workflows
   * Tests
   * Logs
   * README files
6. Confirm Parts 1–5 tests pass.
7. Confirm frontend build passes.
8. Identify current Node version.
9. Select and document supported Node LTS.
10. Summarize implementation plan.
11. List expected files to create or modify.
12. Identify deployment limitations.
13. Do not make deployment claims without evidence.

---

# After Implementation

1. List all created files.
2. List all modified files.
3. Explain Docker architecture.
4. Explain selected Node version.
5. Explain migration strategy.
6. Explain CI/CD workflow.
7. Explain security scanning.
8. Explain deployment foundation.
9. Explain backup and restore.
10. Explain rollback.
11. Explain monitoring.
12. Run backend lint.
13. Run backend tests.
14. Run migrations.
15. Check migration status.
16. Run frontend lint.
17. Run frontend tests.
18. Run frontend production build.
19. Build backend Docker image.
20. Build frontend Docker image.
21. Validate development Compose.
22. Validate production Compose.
23. Start development Compose.
24. Verify container health.
25. Run smoke tests.
26. Run secret scan.
27. Run Trivy if available.
28. Verify backup script.
29. Perform disposable restore test where practical.
30. Verify no secrets are exposed.
31. Report registry publishing status.
32. Report staging deployment status.
33. Provide exact Windows CMD commands.
34. Provide Linux deployment commands.
35. List remaining manual steps.
36. Do not start another phase.

---

# Suggested Local Commands

Backend:

```cmd
cd /d D:\Workspace\professional\Interview_Tasks\Orfus\Payment-Gateway\backend

npm install
npm run lint
npm test
npm run db:migrate
npm run db:migrate:status
```

Frontend:

```cmd
cd /d D:\Workspace\professional\Interview_Tasks\Orfus\Payment-Gateway\frontend

npm install
npm run lint
npm test
npm run build
```

Root:

```cmd
cd /d D:\Workspace\professional\Interview_Tasks\Orfus\Payment-Gateway

docker compose config
docker compose -f docker-compose.prod.yml config

docker build -t payment-gateway-backend:local ./backend
docker build -t payment-gateway-frontend:local ./frontend
```

Start development Compose:

```cmd
docker compose up -d
docker compose ps
docker compose logs --tail=100 backend
docker compose logs --tail=100 frontend
docker compose logs --tail=100 postgres
```

Stop:

```cmd
docker compose down
```

Do not delete volumes unless explicitly requested:

```cmd
docker compose down -v
```

must not be run automatically.

---

# Suggested Linux Commands

Example only:

```bash
docker compose -f docker-compose.prod.yml pull

docker compose -f docker-compose.prod.yml up -d postgres

docker compose -f docker-compose.prod.yml run --rm backend npm run db:migrate

docker compose -f docker-compose.prod.yml up -d backend frontend reverse-proxy

docker compose -f docker-compose.prod.yml ps
```

Do not claim these commands were executed unless they actually were.

---

# Final Codex Output Format

Provide:

```text
## Summary

## Repository Inspection

## Implementation Plan

## Files Created

## Files Modified

## Existing Parts 1 to 5 Preservation

## Node Version

## Backend Docker Image

## Frontend Docker Image

## Docker Ignore

## Development Docker Compose

## Production Docker Compose

## Environment Management

## Database Readiness

## Migration Strategy

## Seed Strategy

## Health Checks

## Graceful Shutdown

## CI Pipeline

## PostgreSQL CI Service

## Frontend Build

## Docker Build Workflow

## Image Publishing

## CodeQL

## Dependency Scanning

## Trivy

## Secret Scanning

## Deployment Foundation

## Reverse Proxy

## HTTPS

## CORS

## Logging

## Monitoring

## Alerting

## Database Backup

## Restore Verification

## Rollback Strategy

## Smoke Tests

## Operations Runbook

## Secret Rotation

## Security Hardening

## Backend Tests

## Frontend Tests

## Docker Verification

## CI Verification

## Security Verification

## Deployment Status

## Windows CMD Commands

## Linux Deployment Commands

## Remaining Manual Steps

## Known Limitations

## Final Project Readiness
```

Do not claim:

* Docker image build passed unless it was executed successfully.
* CI passed unless the workflow or equivalent commands were verified.
* Registry publishing succeeded unless an image was actually pushed.
* Staging deployment succeeded unless a real target was used.
* Backup succeeded unless a backup file was created.
* Restore succeeded unless restored into a disposable database and verified.

Report unavailable tools or credentials as:

```text
Not configured
Not available
Not verified
```

Do not expose secrets.

Do not make unrelated application changes.

Do not start another phase.
